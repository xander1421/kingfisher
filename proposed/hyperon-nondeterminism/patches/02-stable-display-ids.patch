diff --git a/lib/src/metta/runner/builtin_mods/fileio.rs b/lib/src/metta/runner/builtin_mods/fileio.rs
index e197f44..15b41df 100644
--- a/lib/src/metta/runner/builtin_mods/fileio.rs
+++ b/lib/src/metta/runner/builtin_mods/fileio.rs
@@ -89,9 +89,13 @@ impl Grounded for FileHandle {
     }
 }
 
+impl FileHandle {
+}
+
 impl Display for FileHandle {
     fn fmt(&self, f: &mut Formatter) -> std::fmt::Result {
-        write!(f, "FileHandle-{:?}", self.0.as_ptr())
+        // no address: Display reaches hashed output
+        write!(f, "FileHandle")
     }
 }
 
diff --git a/lib/src/metta/runner/builtin_mods/random.rs b/lib/src/metta/runner/builtin_mods/random.rs
index a128d56..1f738a6 100644
--- a/lib/src/metta/runner/builtin_mods/random.rs
+++ b/lib/src/metta/runner/builtin_mods/random.rs
@@ -59,9 +59,13 @@ impl Grounded for RandomGenerator {
     }
 }
 
+impl RandomGenerator {
+}
+
 impl Display for RandomGenerator {
     fn fmt(&self, f: &mut Formatter) -> std::fmt::Result {
-        write!(f, "RandomGenerator-{:?}", self.0.as_ptr())
+        // no address: Display reaches hashed output (incl. error atoms)
+        write!(f, "RandomGenerator")
     }
 }
 
diff --git a/lib/src/space/grounding/mod.rs b/lib/src/space/grounding/mod.rs
index 78259d5..fa50f81 100644
--- a/lib/src/space/grounding/mod.rs
+++ b/lib/src/space/grounding/mod.rs
@@ -220,11 +220,17 @@ impl<D: DuplicationStrategy> Debug for GroundingSpace<D> {
     }
 }
 
+impl<D: DuplicationStrategy> GroundingSpace<D> {
+}
+
 impl<D: DuplicationStrategy> Display for GroundingSpace<D> {
     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
         match &self.name {
             Some(name) => write!(f, "GroundingSpace-{name}"),
-            None => write!(f, "GroundingSpace-{self:p}")
+            // Display reaches hashed output; an address is not reproducible across
+            // processes, so an unnamed space carries no identity here. Debug
+            // still prints the pointer for interactive use.
+            None => write!(f, "GroundingSpace")
         }
     }
 }
