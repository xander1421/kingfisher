# SUPERSEDED — see `proposed/hyperon-match-arity/`

Merged. That report now carries both defects: the `TrieKey::value()` precedence
error (with the patch originally attached here) and the 1023-child arity limit,
correctly separated.
