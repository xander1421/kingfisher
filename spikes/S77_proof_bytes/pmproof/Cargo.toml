[package]
name = "pmproof"
version = "0.1.0"
edition = "2024"

# elders/ is UNTRUSTED cloned code (MISSION_LOOP §10): built and tested in place
# via a path dependency, never vendored or modified.
[dependencies]
pathmap = { path = "../../../elders/PathMap", features = ["counters"] }
