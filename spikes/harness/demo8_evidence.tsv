# MISSION_LOOP §8 -> the artifact somebody claims closes it.
# key<TAB>spike dir<TAB>who asserted this row<TAB>what the artifact itself claims, INCLUDING what it does not cover
#
# A row here is an ATTRIBUTED CLAIM, not a verdict. `demo8.py` checks that the
# named directory exists, is tracked, and certified green -- never that it closes
# the line. The fourth column is where a row states its own limit, and a row
# whose fourth column does not state one should be suspected (A22: this mapping
# is self-authored).
#
# The five §8 items with no row here are UNPROVEN and that is the honest state:
#   * 3 physical devices -- the chain ran 3 HOSTS and 1 phone, not 3 devices
#   * ConceptNet slice via content-addressed shards -- SHARPENED H83: it is not
#     that the slice is unverified, it is that ConceptNet appears in ZERO files
#     outside §8 itself. The corpus actually used is FB15k-237 (spikes/S52_realkg,
#     21 files). Either §8 is stale or a leg is missing; an agent editing the
#     acceptance item to match what was built is weakening a gate to pass it, so
#     this stays UNPROVEN and the question is in HUMAN_NEEDED.md
#   * versioned ban surface, BUILD-ENFORCED -- CORRECTED 2026-08-17 by AGENT-1,
#     one cycle after writing the line above, which was WRONG. It named
#     `admission.py` (refuted as a gate: its predicate rejects 51% of hyperon's
#     own corpus and cannot express mechanism 2 at all). That is a DIFFERENT
#     FILE. The ban surface is `spikes/harness/bansurface.py`, and it is sound:
#     the nondeterministic op surface is ENUMERATED FROM THE SHIPPED BUILD with
#     a source citation (random.rs:99-132), not guessed, and M1.8b measured why
#     it is a safety control rather than hygiene -- quorum-of-3 accepts a
#     genuinely nondeterministic job 21.5% of the time, because two honest
#     devices coincide by chance. So the item is NOT blocked on a broken rule.
#     What is unestablished is the words BUILD-ENFORCED: `bansurface.admit()` is
#     called at RUNTIME by M2_1_fleet/fleet.py, M2_1_fleet/sweep.py and
#     M1_7_transport/run.py. Runtime admission is not build enforcement, and the
#     gap is the enforcement point, not the surface.
#   * quorum-3 with STAKE-WEIGHTED seat draw -- the run is quorum-4 and
#     specs/D3_economics.md publishes no stake floor, so the draw does not exist
#   * a written run-book -- no such document is in the tree
injected cheat	spikes/S26_cheat_attribution	AGENT-1	the injected-cheat half only: 200/200 cheats caught and attributed to exactly one worker over the committed 64-program 4-worker run, denominator 50 not 64. DOES NOT COVER the "paid in points" half, which S26 leaves open because specs/D3_economics.md publishes no stake floor and any slashing number would be invented (A26).
Witnessed verification	spikes/S36_witnessed_job	AGENT-1	one job class (8-byte prefix range query, 37 jobs): replication reads consensus from two non-independent liars 0/37 while a single witnessed verifier rejects 37/37. DOES NOT COVER MeTTa reduction, and its own ATTACK.md found the COMMITTED verifier accepts a deeper-prefix replay 37/37 -- the line is closed only by the q-bound verifier, whose upstream lift is the open row S37.
run-book	spikes/S38_runbook	AGENT-1	documents what a stranger can reproduce from a clean checkout and names what they cannot; `python3 spikes/S38_runbook/check_runbook.py` EXECUTES 10 of its 13 commands and refuses on any command that is neither executed nor excused with a stated reason. DOES NOT COVER a clean checkout -- the commands run in THIS tree, so a command depending on local state would pass here and fail for the stranger; and only fenced ```sh blocks are checked, so §5's inline remedies were resolved by hand.
