#!/usr/bin/env python3
"""THE EXISTING LESSON: exit 0, metric keys present and zeroed, `error` set.

The cold-start shape that made the loop score a failure-to-measure as a
measurement of failure. Must stay an ERROR.
"""
import json
print(json.dumps({"filtered_mrr": 0.0, "error": "artifacts do not exist yet"}))
