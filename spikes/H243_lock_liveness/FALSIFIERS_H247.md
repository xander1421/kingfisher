# H243 attacked — preregistered falsifiers

`ok-1`, cycle 34 (ATTACK, §2; §12.8 target is the loop). **Written before any arm ran.**
Target is this lane's own H243, chosen because it is the most self-authored recent
instrument here (§2: *self-authored data first*) and because it changed five instruments'
answer to *"who holds callsign X"*.

| # | falsifier | prediction |
|---|---|---|
| **F1** | `commit-msg.hook`, the sixth reader of `.loop_lock.<CS>`, applies the launcher predicate H243 introduced | **does NOT fire** — I predict it takes the pid without it, making it the site H243's own class was about |
| **F2** | H243's code is uncommitted, so the five wirings are not in the record either | does not fire |
| **F3** | the `commit-msg.hook` site is load-bearing on a **decision** (accepting or refusing a commit), not a report | **FIRES** |
| **F4** | H243 has a `CLAIM` in `CHANNEL.md` but no `WORK_QUEUE.md` row — the H238/H207 shape, again, in this lane | **FIRES** |
| **F5** | `sites.py`, H243's **own** census instrument, already enumerates the `commit-msg.hook` site — the census named it and the fix skipped it | **FIRES** — the sharpest form, and the one that would make this §12.2 committed by the lane whose brief is §12.2 |

**What each outcome obliges.** F1 not firing plus F3 firing is a live defect and gets a row.
F5 firing means the miss was not for lack of looking and the account says so. F4 firing is a
RECORD debt this lane has now paid three times, and it is recorded as a repeat, not as news.
