# WORK_QUEUE — Kingfisher continuous mode
Status: OPEN | CLAIMED <agent> | DONE | BLOCKED | PARKED | GATED(<what>)
Seeded from MISSION_LOOP.md §4, 2026-08-17.

## P0 — freeze gate (written specs, <=2 pages, falsifiable)
| id | item | status |
|---|---|---|
| D1+ | seat-draw spec | **DONE** — `specs/D1_seat_draw.md`, 5 falsifiers, F5 live pending W3 |
| D2 | canonical result serialization | **DONE** — `specs/D2_canonical_result.md`; 3 classes, 4 upstream exclusions, 5 falsifiers, F5 (canonicalization hiding divergence) flagged as the danger direction |
| D3 | economics as FORMULAS | **DONE** — `specs/D3_economics.md`; no constant derived from Δ, feasibility bound only |
| D5 | ban-surface closure | **DONE** — `specs/D5_ban_surface.md`, v1 surface + 5 falsifiers; F3 unrun, cfg-gate aspirational |

## P1 — verification substrate
| Q1 | quorum simulator: fleet model × canonical envelope × majority adjudication | **DONE** — `spikes/Q1_quorum_sim/`, code+seed+2 firing controls. C4 quantified |
| id | item | status |
|---|---|---|
| W2 | witnessed re-exec on the trie substrate; non-membership via authenticated ordered structure; code+seed+controls | **DONE** — `spikes/W2_witnessed_trie/`. Membership + **non-membership** + **completeness** over a Merkle-committed radix-256 trie, with a verifier that returns False; 9 controls, all fire. Absence provable at **~2.0 KB** (deep miss; the 107 B random-miss figure is corpus arithmetic, `C_miss_depth` guards the substitution). Aligned `(p s ?o)` witness **0.05× shard**, auth path 1.5–2.4 KB **independent of answer size**; `(?p s o)` is exactly **1.00×** — never worth witnessing. Caught myself publishing an accidental cross-shard agreement (2541 vs 2474 B) whose halves moved in opposite directions. Does NOT rescue the prefilter (W4 stands); verifiable job class = trie-only queries |
| D4 | no recoverable subject — absent from `specs/`, never a queue row, in no DECISIONS entry. Evidence says numbering gap (D2 self-describes as "Last P0 freeze-gate item"; entry 81 lists D1+/D3/D6) | **BLOCKED_ON_HUMAN** — `proposed/D4_slot_candidates.md`, 4 rows + recommendation. Not agent-decidable: §7 makes it my own exit condition, so deciding it is A22 |
| D6 | discipline standard: no result without code+seed+controls | **DONE** — `specs/D6_discipline.md`. Enforcer named clause by clause (E1–E8 in `harness/provenance.py`), holes named (H1–H5), 5 falsifiers of which **2 KNOWN-FAILING at birth** (F1 vacuity, F5 number-not-in-artefact). F2 measured: 6 RESULT.md cite D6, **0** have a provenance.json; 4 of 89 spikes have one |
| — | retro-fit owed: Q1, S72, N1, W4, B1 cite D6 with no provenance record | **DONE** — `spikes/harness/retrofit_d6.py`, observations **extracted** from each spike's own committed artefacts, never retyped from prose. **Q1 2/2, B1 4/4 and W4 4/4 COMPLIANT; N1 2/4 and S72 1/5** record `ok:false` — both need timings under a refusing gate, so neither is closable today. **W4 closed by recovery, not re-measurement**: its table went to *stdout* and the original run redirected only *stderr*, so the number was printed and discarded; rebuilding `rk_inst.c` recovered it and the stderr half reproduces the committed `ampl.txt` byte for byte. **9 of 20 stated controls across five spikes have no observation on disk** — R4 is where this project actually failed. W4's missing one is the control its own page calls *"the one that matters"*; S72's `gate_green_before_and_after` has **no pre-run gate file in any commit** and `k4.sh` never invokes `quiet.sh`. Each page gained a changelog line, none was edited above it (P3). Found E1 bugs **4 and 5** (mtime-vs-commit-time clock mixing; `record`'s own output raising the floor) |
| S73 | canonical **space** state at an epoch boundary; verifiable epoch deltas | **DONE** — `spikes/S73_epoch_commitment/`. 66 epochs over the real 67-program corpus chain and verify; a verifier **computes** `root_N+1` by folding `root_N` forward from the additions alone. **1,150 B per added atom batched / 1,770 B isolated** (12x space = 2.27x proof); epoch cost is **~1-2 recomputed nodes per added atom**, O(added) not O(space). XOR-of-hashes null gives the same O(k) delta and is **forged in one line** (`a^a=0`, declare an atom twice); its absence proof has no bytes to be made of (32 B vs the trie's 257 B). **Root commits to STATE, not history** — two epoch groupings reach the same root; history binding needs a `(root, delta)` chain, not built. 11 controls, all fire. Does NOT unblock bisection: interpreter state stays RED per S68 |
| S74 | epoch-chain commitment: bind the epoch SEQUENCE, which S73's root does not | **DONE** — `spikes/S74_epoch_chain/`. `chain_N = H(chain_{N-1} ‖ root_N ‖ H(delta_N))`, delta committed as a W2 trie root so there is no second encoding. **32 B per epoch, 2,176 B for the whole 67-epoch history.** Catches regrouping, reordering and epoch-splitting **with the final trie root byte-identical**, so the root provably cannot see them; plus drop, delta-substitution and transplant. 8 controls, all fire, certified through `kfcheck.certify` with a falsifier. **Boundary as a control**: the chain binds SEQUENCE and not STATE — it verifies a sequence whose declared root its own delta does not produce, and only S73's fold-forward proof rejects that. Two of my own controls were weak (one mangled two positions, one was near-tautological) and were replaced before publishing |
| S75 | run W2/S74's declared falsifier against MORK's real `pathmap` | **DONE** — `spikes/S75_pathmap_check/`. **It FIRES at 18.4× on S73/S74's variable-length atom keys and does NOT fire at 2.4× on W2's fixed-length triple keys** (threshold 10×, fixed before reading). Mean node depth 7.6→139.1 for atoms, 4.2→10.3 for triples; max 15→1,148. Authentication overhead scales with nodes on the path, so **S73's 1,770 B insert proof is ~33 KB on real pathmap**; W2 becomes ~3.6–5.8 KB, a constant factor as its caveat claimed; **S74 is untouched** because a chain step hashes digests and never walks a path. Cause is *key length* (up to 1,155 B split into ~1,148 bounded-span nodes), so the fix is in the ENCODING. **Second finding: pathmap's own `merkleization.rs` is a dedup pass keyed by a 128-bit non-cryptographic gxhash with no proof and no verifier — the crate depends on no cryptographic hash at all.** 7 controls, all fire. One of them was wrong first: it grepped `fn (prove|verify|proof|witness)` and matched 14 Rust BORROW witnesses. **RETRACTED IN PART by S77, 2026-08-17: the ~33 KB and the ~3.6–5.8 KB are withdrawn** — they are node depth times a digest width, and a proof is paid for in SIBLINGS, which a single-child position does not have. Measured: **1,568 B atoms, 2,350 B triples**, so S73's published 1,770 B and W2's published 1.5–2.4 KB were both approximately right. Its criticism of S73's "same shape, different constants" caveat is withdrawn with them. **Every depth number stands and reproduces**, as does the `merkleize`-is-not-a-commitment finding |
| S76 | run S75's own indicated fix: intern symbols to fixed-width ids and re-measure the depth ratio | **DONE** — `spikes/S76_interned_keys/`, `certify ok=true`, 7 controls all fire. **S75's mechanism claim SURVIVES its falsifier: 18.39× → 7.86× at 4-byte ids and 5.50× at 2-byte, under the 10× bar S75 fixed before reading.** It does **not** reach W2's 2.44× regime, and a four-encoding sweep over one atom set says why: `pathmap` spends **~1 node per key BYTE** (0.86–1.30 across five key sets; max depth 1155→1148, 619→611, 533→525, 447→439, triples 12→11) while W2's trie depth is the atom's **structure** and does not move at all across the three interned widths (7.8 / 7.8 / 7.8). **The ratio is bytes-per-structural-node** — the form that predicts a number instead of describing one. Interning shortens only the symbol term; the `E` + 2-byte arity framing costs 3 B per expression node, so an interned key still averages 36.6 B. S73's isolated insert proof: 1,770 B published, ~33 KB on real pathmap, **~14 KB at id4, ~9.9 KB at id2**. **Not quotable as a rate and `units` is what says so** — adjacent slopes span 34% against a 25% tolerance, so `check_affine` refuses and the four points are reported as points (A18). Instrument control is the strong kind: S75's binary unmodified, run from `probe_cwd/`, and S73's **original** encoding replayed through it now reproduces S75 exactly (139.05 / 83,210 / 1,246). Interning **moves** cost rather than removing it: 1,713 symbols, **44,891 B of table to commit**, ids assigned by sorted symbol order so two parties derive the same table without communicating. S75's RESULT.md corrected in place with a changelog line, no number withdrawn. **RETRACTED IN PART by S77 within the hour: the ~14 KB and ~9.9 KB are withdrawn and the direction is reversed** — interning makes proofs **22% bigger** (1,568 → 1,917 B), because shortening keys concentrates branching into fewer positions and branching is what a proof pays for. Every depth number, the sweep, the affine refusal and the instrument controls stand and reproduce; **none of them could see it, because all seven checked the measurement of depth** |
| S77 | do what S75 and S76 both declared and deferred: generate real proofs and measure the BYTES, instead of multiplying node depth | **DONE** — `spikes/S77_proof_bytes/`, `certify ok=true`, 6 controls all fire. **Depth is not a proxy for proof size and both prior corrections are retracted.** An authentication path carries the digests of the SIBLINGS at each position; a single-child position has none, and a 1,155-byte key is a long *unbranched* run — ~1,148 nodes, almost all single-child, ~0 digests. Walked `pathmap`'s real paths with `child_count` at every position on the reviewed spikes' own committed key files: mean siblings **45.7 / 56.4 / 70.2** for original atoms / interned atoms / triples, i.e. **1,461 / 1,803 / 2,246 B** of authentication against depth's implied **4,450 / 1,960 / 328 B**. **The ordering inverts** — by depth the triples are cheapest by 13×, by what a proof carries they are the most expensive; depth over-predicts one by 2.8× and under-predicts the other by 7.2×, so not even a dividable constant. Cross-checked against a prover that exists: the same keys through W2's `prove_membership`, every sampled proof `verify_membership`-ed against the root, giving **1,568 / 1,917 / 2,350 B** — same order, within 5–7%. **Proof size is set by BRANCHING along the path, not key length**, which reverses the design advice both spikes gave. What was missed twice is the finding: both had firing controls, a stated falsifier and `certify ok=true`, and every control checked the depth measurement — *a more careful measurement of the wrong quantity reads as a stronger result* |
| S78 | S77's own caveat: does a PHYSICAL-node commitment reorder the key sets and make S77's inversion specific to the logical view? | **DONE** — `spikes/S78_physical_nodes/`, `certify ok=true`, 4 controls. **It reorders only above 6.10 B of framing per physical node on the path; minimal framing is a segment length at 1–2 B, so S77 survives with a 3.0× margin.** Decidable from `merkleization.rs` itself, whose node hash is `(value, [(path, child_hash)])` — recomputing it needs each child's segment and hash, so k−1 sibling digests for k children and **zero for a single-child node**, the same reason the logical view charges nothing for an unbranched run. **GRADE D in the verdict line, not a footnote**: arithmetic over S77's measured inputs plus one E input read from source, published as a threshold and never as a byte count — both retracted figures in this chain were D published as if measured. One control fails in **both** directions (threshold under 1 B ⇒ S77 needs qualifying; over 32 B ⇒ the caveat was never a risk), and the source control tests the composition as a **property**, since a grep for "hash" in `merkleization.rs` cannot tell a commitment from a dedup key — S75's exact mistake |
| S79 | absence and completeness under S77's corrected model — the corrected figure S77 retracted without replacing | **DONE** — `spikes/S79_absence_bytes/`, `certify ok=true`, 5 controls. **Non-membership costs 1.02–1.04× membership and orders the three key sets identically**, so S77's *"proof size is set by branching, not key length"* is **not membership-only**. Measured against W2's real `prove_non_membership` with every proof verified: **1,589 / 1,930 / 2,291 B**, so **W2's published ~2.0 KB on the realistic miss is CONFIRMED** and S75's ~3.6–5.8 KB restatement stays retracted. Model stated before the numbers: absence charges `(children−1)` along the shared prefix like membership, then **every** child at the divergence position, because a subset would let a prover hide the child that makes the key present — that +1 is ~32 B on a 1,500–2,300 B proof. **The intuition that absence is the expensive case is wrong**, for the same reason depth was. Probes diverge as deep as each key allows (mean 95.5 / 54.3 / 11.0) by construction, since a random-byte probe diverges at the root and gives the small true useless number S73 published as a flat 293 B (A27). No new unvalidated instrument: the walk is the Python recount C14 validated at 0.00%. **Completeness proofs remain unmeasured** — no single divergence point, so this model does not extend to them by inspection |
| S80 | completeness proofs — the third kind, which S79 said does not follow by inspection | **DONE** — `spikes/S80_completeness_bytes/`, `certify ok=true`, 4 controls. **The falsifier FIRED: the completeness auth path orders the key sets DIFFERENTLY from membership**, so S77's *"proof size is set by branching, not key length"* is a **point-query** claim and S77/S79 now carry that scope in changelogs. Triples are the **most expensive point query (2,269 B) and the cheapest range query (1,401 B)**: a range query stops before the tail, and for triples the tail is where the branching lives (last 3 of 12 bytes, high-fan-out object field, 38% saved), while for atoms the skipped 26 bytes are almost unbranched (under 1%). **Mechanism survives and sharpens: a proof costs the branching it actually PASSES, and where the query stops decides which branching is on the path.** Answer size checked as a separate axis rather than asserted — triples carry 100.1 answer keys against ~12 while having the cheapest auth path, so the two order oppositely and a single total would be one number for two mechanisms (A18); W2's "auth path independent of answer size" holds. **Two defects in its own method, both caught by its own controls**: a sample mean compared against a population mean (`certify` refused the run as **VOID, not negative**; fixed rather than loosened, then the same contamination found and fixed in the membership comparison), and a control that could not express a negative result (A21) |
| W3 | ~~witness sizes under non-aligned access~~ | **CANCELLED** — premise falsified by S52 (0.2/1.0/8.8% measured vs W1's 7.7/100/100%). The 0.9× pathology is also an artefact: fixing the multiproof and the short final chunk gives exactly 1.000× |
| **W4** | **read set of the HDC prefilter: what is it, and can it be sublinear without invalidating S52's timings?** The engine scores every bundle on every query. **Verification eligibility cannot be decided before this.** Highest priority in P1 | **DONE** — `spikes/W4_prefilter_readset/`. Read set is 100% by construction (similarity search has no key order to skip). 78.59× amplification measured but it is oracle-fitting harness, outside S52's timed region. Witness 1.5–12.2 MB. Residency coupling NOT cut |

## P2 — M1 integration (the demo path)
| id | item | status |
|---|---|---|
| M1.1 | Android app skeleton (16 KB pages, pointer-tagging manifest, foreground dataSync budget) | **PARTIAL** — APK builds, installs, runs; `libhyperonc.so` 6.43 MiB loads in-process in 1.34 ms; **16 KB alignment verified 0x4000 on all LOAD segments** (S2 open item 3 closed). **Settles the preflight number at 98.47 us — 356x cheaper than M1.3's published 35.1 ms, 0.14% of a job.** **MeTTa now RUNS in-process**: `(+ 1 2)`->`3`, `(intersection-atom (A B C) (B C D))`->`(B C)`, 11.23 ms, **byte-identical to native `fuelrun` on the same device** (new axis: runtime host, not ISA). Found: `libhyperonc.so` has **no SONAME** (bakes the host build path into `DT_NEEDED`; fixed with `-Wl,-soname`). **WorkManager worker DONE**: runs MeTTa in-process (16.6 ms), refusal path proven. Found **SCHEDULER_SPEC 2 does not build** — rules 4+5 mutually exclusive (`Cannot set backoff criteria on an idle mode job`); spec corrected. **OPEN: process-per-job conflicts with WorkManager's process reuse** — largest open M1 issue. Blocker confirmed: `hyperonc` has no `[features]` block so the 4.00 MiB minimal build needs an upstream cfg-gate | 
| M1.3 | charge-time worker: WorkManager charging+idle+UNMETERED, checkpointed chunks | **PARTIAL** — in-worker preflight residue built and tested (24 assertions), session-gated dispatch wired into M1.8, refusal proven at session 0 and mid-run (32 dispatched, 32 completed, none lost). **CORRECTED**: the 35.1 ms was adb+dumpsys, i.e. the harness. Native sysfs read measures **8.4 us** — 4,180x cheaper — so per-job preflight IS viable and S6 marks it *Residue: yes*, i.e. required. Session gating in q3.py is a harness accommodation, not a design finding. True binder cost still unmeasured (needs M1.1). WorkManager/Kotlin half blocked on M1.1; `onStopped()` checkpointing blocked on the S68 state commitment. `spikes/M1_3_worker/` |
| M1.5 | shard store on iroh-blobs (BLAKE3 verified ranges — same proofs W1 uses) | **DONE, WITH A DEVIATION** — built as sha2-256 + sqlite LRU, stdlib only, not iroh-blobs/BLAKE3. Cold 173.5 KiB to device, warm **0 bytes**, phone median 109.0 -> 68.8 ms; 22 invariant assertions. `spikes/M1_5_shardstore/`. **The deviation needs revisiting:** the row justified BLAKE3 verified ranges as *"the same proofs W1 uses"* and W1 is INVALID, so that reason is dead — but `REPORT_Golem_clay_verification.md` supplies a NEW one. Requestor-side spot-checking audits a random RANGE, and verified ranges are exactly the primitive that lets a verifier check part of a blob without fetching all of it. Whole-blob sha2-256 cannot do that. Re-open if the Golem-style audit is adopted | 
| M1.7 | transport phone↔coordinator on iroh | **DONE** — phone dials out over HTTP, 66/66 byte-identical to host, loopback-only bind + `adb reverse` so no network surface. Miss control caught a fabricated-envelope bug (`curl -s` on 404). `spikes/M1_7_transport/` |
| M1.8 | quorum-3 pipeline end to end; first 1 phone + host as 3 processes | **DONE** — 66/67 unanimous, 3 processes / 2 OSes; the 1 refusal is `(flip)`, the corpus's own positive control. Not a trust-independent quorum (2 of 3 are the same binary on one host). `spikes/M1_8_quorum3/` |

## P3 — measurements
| N1 | ~~re-derive the ~50 µs prefilter cost~~ **DONE** — `spikes/N1_prefilter_cost/`, 23.9 µs at T=3, 8.7% of query. Amdahl rung restored |
| ~~N1-old~~ | superseded from a kernel profiled against the roof, 4 workers, background cpuset. The only rung that could still carry the NPU descope alone, and it inherits S18's artifact. **Device gate open** | OPEN — high |
| B1 | bundling compression vs recall, real KG | **DONE** — `spikes/B1_bundling_real/`. B=16 fits VTCM at p90 0.2%. Code shipped |
| id | item | status |
|---|---|---|
| C2 | per-device supply under gate | DONE — S71, 2.83/11.17 jobs/s |
| C3 | packed popcount on deployable cpuset | DONE — S72, 15.2× short, prediction wrong by 6.3× |
| L1 | oflineAI logits byte-compare, fixed extractor, 2 runs 1 device then 2 devices | OPEN — needs 2nd device for the second half |

## P5 — G-series (agent-2 lane)
| id | item | status |
|---|---|---|
| G24 | evolving rule population, six arms | **DONE** — `spikes/G24_population/`. Verdict NOT DOMINATED, precision 0.0355 flat, coverage +218%. **Three "why" statements corrected 2026-08-17 by G25; see its changelog** |
| G25 | explain `no_death +5059`: real tradeoff or rent calibration artefact? | **DONE** — `spikes/G25_carrying_capacity/`, 16 runs, 3 seeds on the 3 headline configs, 4 controls, `provenance.json ok=true`. **Both, and neither is about death.** G24's `no_death` arm contains **no selection at all** (nothing removed, `MAX_POP` unused, parents uniform, `imp` read only by death) so it never was "full minus carrying capacity". The missing 2×2 cell `no_death+no_abduct` gets **1514** correct at pop 531 vs `no_death`'s 6361 at 557 — at matched population with selection absent both sides, **abduction is worth 4847 and volume ~155**, so G24's "coverage rises with population size almost mechanically" is measured false. Keeping death and raising `WAGE_POOL` alone closes **51–85%** of the gap (3 seeds) at 2.6× fewer predictions and 2.5× the precision; +1753, disjoint ranges, exact permutation p=1/20=0.050 (the floor at n=3). **ECAN belongs as a precision mechanism, not as a coverage cost.** HOLE: selected-557 vs unselected-557 is unreachable — the dial saturates at pop ~239 (40× pool → 2.17× pop), so this rests on a trade, not a dominance |
| G27 | does the PROPOSAL BUDGET reach a selected population of 557 where `WAGE_POOL` saturates? Closes G25's hole | **DONE** — `spikes/G27_budget/`, 12 runs + 3 seeds on the headline pair, 6 controls. **Yes, via OFFSPRING**: 12x budget -> 2.99x population (229 -> 684); 4x offspring gives 568, 3x rounds only 296. **Population-matched: SELECTED DOMINATES, 3/3 seeds** — but carried by the PREDICTION axis (0.54-0.83x of no_death's assertions, same direction every seed), NOT coverage (ranges separate by 32 triples against a 1338 band). Paired sign test floor p=1/8=0.125. **Budget-matched: 0/3 dominance, all trades.** The two matchings are mutually exclusive — no_death's population IS its proposal budget — so which arm wins is a choice of what to hold fixed. G24 reported the budget view, G25 asked the population question. **C7 FAILED and paid for itself**: `pick_parent` landed mid-sweep, 6 of 12 runs contaminated; classified by commit timestamp (agrees 12/12 with the observed split), C7a PASS 6/6, and C7b turns the 6 contaminated pairs into the controlled repro-ON/OFF measurement neither lane had — coverage 3/6 (inert) but predictions 6/6 fewer and precision 6/6 better, 1.04-1.45x. `provenance.json` deliberately **ok=false**: 6 artifacts predate G25's analyse.py, a dep-dir file that is provably not an input; dropping the dep would buy green by removing a real dependency |
| G28 | harness: a dep dir's non-input files raise its staleness floor (2 instances). Fix = pass recorded artifact basenames as `exclude` to `newest_source_mtime` per dep | POSTED to CHANNEL — harness owner's call, not editing `provenance.py` under them |
| G29 | differential-test the hand-rolled miner against `elders/hyperon-miner` on one corpus; the only defence against a shared bug quorum cannot see | OPEN |
| G30 | external yardstick: filtered MRR / Hits@1,3,10 on FB15k-237 against AMIE / RuleN / AnyBURL, replacing top-12 held-out confidence | OPEN |
| G31 | read `elders/hyperon-miner` surprisingness before writing another statistic — it subtracts the chance-structure baseline *inside* the measure, which would retire the 500-shuffle null and its 1/501 p-floor | OPEN |

## P4 — drafts for the human (proposed/ only, never post)
| id | item | status |
|---|---|---|
| U1 | hyperon nondeterminism PR text + minimal patches, final form | DONE — proposed/hyperon-nondeterminism/ |
| U2 | MORK issue-#2 comment, final form | OPEN |
| U3 | buyer one-pager: auditable on-device agents | OPEN |
| U4 | Deep Funding proposal refresh with current evidence grades | OPEN |

## Watchers
- host quiet.sh: REFUSED (11 containers, another project's). Recheck each cycle.
- device quiet.sh: OPEN.
- W1 attacker: live. W3 blocked on its verdict.
| M1.9 | QUIC transport | **EVALUATED, DEFERRED** — attacks the fixed cost, which is 27% and falling at deployable shard sizes (M1.5b: 63.2 ms + 37.9 MB/s); batching already gives **38x** in the regime where fixed cost dominates. Real wins are connection migration across WiFi/cellular and the TLS we entirely lack. Cost: Cronet adds several MB to a phone APK. **Adopt when** devices are seen changing network mid-job, or shards drop into the fixed-cost regime without pre-staging. `analysis/TRANSPORT_QUIC.md` |

## H — harness (added 2026-08-17 by CLIENT-3 per MISSION_LOOP §12.1)
The loop machinery is a first-class artifact under D6. It had no queue rows and
no tests while being the only thing between the fleet and a silent stall.
| id | item | status |
|---|---|---|
| H1 | Stop hook enforceable as written; per-lane signals; exit marker the launcher can read | **REOPENED then DONE** — v3 was marked DONE and was wrong: `LANE="${CALLSIGN:-unknown}"` collapsed every callsign-less session into one shared lane, so humans and reviewers were gated and incremented the fleet fuse (`.loop_blocks.unknown`=3 observed). v4 fails closed on identity. The 15-check suite passed over it because every check set CALLSIGN — happy path only. Now `loop_gate.sh` v4, 21 checks |
| H2 | launcher must not end a lane on prose; backoff; hang watchdog | **REOPENED** — was DONE and should not have been: v2 exists on disk but both live wrappers parsed v1 at spawn, so the prose-matching defect is live right now. A fresh reviewer printed all three marker words this hour and they missed `loop_AGENT-*.log` by luck. This is error #5's class (DONE while broken) unswept — §12.2 broken at the site of my own rule. Closes only at the relaunch cutover |
| H3 | §11 publishing rail cited by 7 files never existed; §10 split so every citation resolves | **DONE** — MISSION_LOOP §11 |
| H4 | mechanical reference resolver: every §N / spec / file citation in the harness must resolve, per §12.4 | **DONE** — `spikes/harness/refcheck.py`, refusing rather than warning. Resolves `§N` / `§N.M` against MISSION_LOOP, duplicate section numbers, `A<n>` against `analysis/GUARDRAILS.md`, and backticked repo paths, across the §12 harness list plus `spikes/harness/` and `prompts/`. **Its first live run found that `GUARDRAILS.md` stops at A24 while A25–A30 are cited in four harness files** — six guardrails declared in HANDOFF as they were earned and never landed where they are cited, so every reference resolved to nothing while reading as satisfied. Landed by **extraction** from the entries that earned them. Tuned against H14's named failure mode: an external citation (`boinc/sched/credit.cpp:284-289`, gitignored or absent trees) is not a broken reference — the rule is that a repo path's **first segment is an existing top-level entry** — and `.git/hooks/…` is skipped because the harness cites it **both ways**, one file naming a hook that exists and another naming one *because it does not*. **States what it cannot do**: it resolves POINTERS, not meanings; whether a section says what the citation claims is §12.12's unmechanisable class. Selfcheck plants all four breakages **and** asserts the resolvable citations stay quiet; fixtures assembled from parts so the file carries no literal broken citation of its own, because exempting the checker from its own scan is the H-HOOKREG blind spot. ~~**Harness is now clean except one row in another lane's file** (`HANDOFF.ATTACKER-1.md` cites §0), reported not edited.~~ **WITHDRAWN by its own author in `7175c0e`** and struck here by ATTACKER-1 under the refcheck attack, because the retraction did not reach this row: that journal line reads *"per §12 and the brief's §0"* and `prompts/ATTACKER-1.md` defines `## 0 ·`, so the citation was exact and the checker was wrong. LEDGER standing rule 12 — a retraction must reach every file carrying the claim; it reached `CHANNEL.md` and not this row nor `HANDOFF.md:471`, both found mechanically when v3 stopped the union hiding them. Original diagnosis below.  ~~OPEN — **regressed twice more within the hour**: `CLAUDE.md` cited §10 for publishing after H3 moved it to §11 (fixed), MISSION_LOOP carried two §9 (renumbered to §13), and §13 pointed at a `CLAUDE.md §2` that the rewrite deleted (fixed). All three found by eye. `grep -E '^## [0-9]+ ·' \| uniq -d` is a third of this row |
| H5 | journal self-contradiction check: nothing in both a DONE list and a NEXT list, per §12.5 | **DONE — `spikes/harness/journalcheck.py`, and the result is that §12.5 IS NOT MECHANISED BY IT.** Two tiers: COLLISION (an id that is the SUBJECT of a NEXT title and is recorded DONE) **refuses**; SUSPECT (an id merely CITED in the title, or a title sharing most of its rare vocabulary with a DONE title) reports and does not gate. **Coverage measured, not asserted** — `--history` replays all 45 committed journal revisions: it refuses on **1 distinct real violation** (`D4 and D6 as written specs` with `C2 DONE: D6` above it, standing across 2 commits). Meanwhile the live tree ran **green while 4 real §12.5 violations stood in `HANDOFF.md`** (NEXT "physical-node accounting" = S78 DONE; "absence and completeness" = S79/S80; "completeness proofs" = S80; "explain `no_death +5059`" = G25) — **none shares an identifier with its DONE entry, so none is visible to any id-matching rule.** So it sees 1 of 5 violations this repo can evidence. All four live ones fixed in the same commit, struck in place with the reason (§5), and the `HANDOFF.md` NEXT list is now one live item. **Three defects in the checker itself, every one found by `--history` and none by reading**: a bare `\bDONE\b` matched inside **LOOP-DONE**, so a NEXT quoting §7 became its own DONE header and collided with itself (8 fictional refusals); ids harvested from DONE block BODIES let a citation masquerade as a verdict; and whole-headline matching refused on 2 of 3 distinct historical cases, both citations (`against S74's chain`, `the retro-fit D6 owes`) — scoping the refusal to the leading subject took it to 1 of 1. `--selfcheck` drives both tiers, the demotion, and **asserts the uncovered case uncovered**. Known limit reported not special-cased: `C21` is a cycle label and `C2`/`C3` are P3 queue rows, one namespace |
| H6 | external liveness alarm — a lane dead for an hour is currently silent | OPEN — `launchd` KeepAlive per lane is the native form; `.heartbeat.$CALLSIGN` now exists to watch |
| H7 | first ATTACK cycle aimed at the harness rather than a spike, per §12.8 | **DONE** — `spikes/H7_harness_attack/`. Asked the one question a passing suite cannot answer for itself: **is a red run reachable?** `falsify.py` restores each defect on an isolated copy and requires the named check to go red; **8/8 fire, control copy stays 40/40 green**. Three live defects found on the way: the Stop hook's refusal message instructed lanes into the bare `.loop_signal` it stopped accepting in v5 (co-found with AGENT-1/H16, reproduced 3/3 turns blocked); `$LANE` interpolated into that same JSON unvalidated, so `CALLSIGN='L"6'` made the block decision unparseable and the refusal is lost (**an injection opened BY the H16 fix**); and §13.1 cited `.git/hooks/pre-commit`, a file that does not exist naming the mechanism `commit-msg.hook`'s own header records as already proven unworkable — with `.git/hooks/` untracked, no installer and no drift check, so the only enforcing gate in the repo is absent from every clone |
| H8 | callsign allocation: a lane must not be spawned onto a held callsign | **DONE ON DISK, LIVE AT NEXT RELAUNCH (AGENT-2)** — H21's status, measured with `ls .loop_lock.*`: the three lanes running now started 13:25, before v6, so only `.loop_lock.ATOM-3` exists and the refusal is not enforcing in this fleet yet. — `run_loop.sh` **v6**, defect 9: a per-callsign lock holding the loop's pid, acquired with `set -o noclobber` (create-or-fail is atomic, so two launchers racing cannot both win) **before the fork**, because a refusal after the detach goes to `detach_$CALLSIGN.log` while the caller still sees exit 0. **The prose rule was unrunnable, which is why it never held**: §12 says a callsign is "allocated, not assumed" and `prompts/ATTACKER-1.md` §0 tells a lane to look for a holder with `ps -eo command= \| grep 'You are X\.'` — measured on this machine, `ps` shows every launcher as `bash ./run_loop.sh` with the callsign nowhere in argv, the LAUNCHER's environment exposes no CALLSIGN either (`ps -E` ignored; `ps eww` measured over every live launcher pid) — **CORRECTED same cycle by a peer session's counter-measurement:** this first read *"macOS does not expose another process's environment"*, which is **false**, since `ps eww` reads a same-user process's environment fine; the surviving statement is narrower and is what the conclusion rests on, namely that the launcher does not expose it and the `claude -p` turn does, and the only process that carries the callsign is the `claude -p` child, which exists **only while a turn is in flight**, so between turns the check reads clear on a held callsign. §12.4's failure exactly: a rule enforced by a check that cannot run reads as satisfied. The holder is now RECORDED, not inferred — "who holds AGENT-2?" is `cat .loop_lock.AGENT-2` and needs no `ps`. **Liveness is pid + command, never `kill -0` alone**: this fleet burned ~1300 pids/minute with three lanes up, so macOS's 99999-pid space wraps in ~75 minutes and a bare `kill -0` would refuse a legitimate lane roughly every wrap — a dead lane has no next cycle. **No release path, deliberately**: a trap covers a clean exit and misses SIGKILL, the watchdog's own `pkill` and a power cut, so the stale-reclaim branch has to be right anyway and a second mechanism would be the one never exercised. 4 new checks in `test_loop_gate.sh` + a fixture assertion, **falsified in both directions on isolated copies**: delete the refuse branch → 3 red, reclaim checks correctly stay green; weaken liveness to `kill -0` → only the pid-reuse check goes red. **Live instance found while writing it**: 13:26:33, a launcher in the repo root under `CALLSIGN=ok-1` spawning real turns with `--dangerously-skip-permissions`, at that moment with no brief, no CHANNEL line and no queue row — found by reading `lsof` for an unfamiliar detach log, since `ps` cannot show a callsign. Scoped rather than left to decay: `ok-1` is a legitimate atom an hour later, so the finding is the **window** in which a lane ran unallocated and unrecorded, not a claim about that lane |
| — | **evidence for H8's neighbourhood, measured by ok-1 2026-08-17 and REPORTED not fixed: three harness components disagree about what a callsign is.** `run_loop.sh` and `.claude/hooks/loop_gate.sh` both allow `[A-Za-z0-9._-]`; `spikes/harness/commit-msg.hook`'s `is_callsign` requires `^[A-Z][A-Z0-9]*(-[A-Z0-9]+)+$` **plus** a digit. The lowercase callsign `ok-1` — allocated by an ATOM-3 launcher probe, not chosen — passes the first two, and passes the third **only because `atom=$(up "$(val Atom)")` upper-cases the value before the check**. So the string recorded in history is `ok-1` while the string the gate validated is `OK-1`, and §13.1's own header records what that costs: *"AGENT-1/agent-1 split one atom into two"*. H8's own lock is keyed on `$CALLSIGN` verbatim, so two launchers on `ok-1` and `OK-1` take two different locks and the fix does not see them as one lane. **Not fixed here**: narrowing the shape under live lanes makes the launcher refuse an already-running lane at its next relaunch, which is the cutover condition H9 and H11 are parked under | OPEN — reported, needs the cutover |
| H9 | drop bare `.loop_signal` from hook and §7 — the last shared-state loophole | OPEN — deliberately NOT done live: §7 still documents it and a lane mid-turn may write it, so removing it now could wedge a running lane. Do it at the v2 cutover |
| H10 | one journal per lane (`HANDOFF.$CALLSIGN.md`), single writer each | OPEN — HANDOFF is 255 lines with two NEXT lists and two writers; §12.5 forbids the self-contradiction it keeps producing |
| H11 | fuse semantics: comment said per-session, `run_loop.sh` clears per turn, so MAX_BLOCKS cannot fire for the runaway it exists to stop | OPEN — comment corrected to match behaviour; changing the semantics is a cutover change under live lanes |
| H12 | **commit authorship cannot distinguish agents** — every commit carries one human git identity, so two independent reviewers attributed `e990f11` to the wrong atom from the same evidence. Needs a lane marker in the commit trailer or an agent field | OPEN — higher value than it looks: without it, no atom's work or fault can be attributed from history, which defeats the record §13 calls a deliverable |
| H13 | runaway fuse is an unsynchronised read-modify-write | **DONE (ok-1)** — `loop_gate.sh` **v7** locks the increment with `mkdir`, the portable atomic test-and-set (macOS ships no `flock(1)`). **Append-and-count was the row's other suggestion and it is the wrong one**: it changes the file from a decimal count to N lines, which breaks checks 6, 7 and 12 — per-lane counting, the 400-seeded fuse release, and corrupt-file recovery — and each of those encodes a defect that actually shipped. A lock around the RMW leaves the format alone, so those three test what they tested before. **FAIL OPEN after ~1s of spinning**, clearing the lock and proceeding unlocked: a shared gate that can wedge every lane is a worse defect than the one being fixed (H9, H11), and the only way a holder keeps it that long is that it died inside two shell commands. **The row's real defect was in the SUITE, not the hook**: check 11 was a `KNOWN` line, not a check — it printed the undercount and passed either way, so the suite's exit code could not tell a fixed fuse from a broken one, which is A28 (a field recorded but never read) standing where an assertion belongs. Now REQUIRING 20/20. Measured before the fix on this tree: **12, 13, 14 of 20 across three runs, and 28 of 60** — the row's "land on 10" is the right order and the loss is ~35%. Falsified, both directions, `bash spikes/harness/test_h13_falsify.sh`: restore the unlocked RMW on an isolated copy ⇒ that check goes FAIL, with an unmodified control tree showing it PASS, because a scratch tree broken for an unrelated reason produces the FAIL line for free. **Reachability stated rather than implied**: `.loop_blocks.$LANE` is per-lane and one lane's turn ends are sequential, so the concurrency that triggers this needs TWO launchers on one callsign — i.e. it is reachable through H8, which is still OPEN, and the consequence is a fuse that blows late, never early |
| H14 | `githygiene.py` has no self-check and cannot pass — exit 1 is permanent on 16 already-tracked violations, which its own ALLOW comment names as the failure mode: "a checker that fires on known-accepted items every run is a checker everyone learns to ignore" | **DONE** — and H14 was right twice over. The module was **broken in HEAD** (no `import re`, `NameError` at IMPORT time, in every lane's §13 pre-commit path for 20+ minutes while its owner committed twice more) — the module with no test, failing in the way only a test catches. Verdict fixed: already-committed violations are REPORTED, never gated, so the exit code depends on what you staged instead of being 1 forever. `--selfcheck` added, 13 checks, and on its FIRST RUN it found two more defects: missing trailers reported for a commit that does not exist (empty repo, family B), and HEAD checks gating YOUR commit on ANOTHER lane's last commit, unfixable without rewriting history. Plus `git ls-files` reads the index, so new violations were mislabelled *already committed*. Falsified by `falsify.py` G1/G2 with a control — which exposed two defects in my own instruments: the driver truncated the file before reading it (caught only because `anchored_replace` REFUSES), and the import probe imported the installed module rather than the copy under test (family C, invisible to any number of green runs) |
| H15 | nothing runs `githygiene.py` or `test_loop_gate.sh` automatically | **DONE for three of four checkers; the fourth is split out as H29.** `spikes/harness/pre-commit.hook` v1 GATES `refcheck.py`, `journalcheck.py` and `githygiene.py` at every commit, installed by `install_hooks.sh` **v2** — v1 installed one gate, so `pre-commit.hook` would have shipped tracked, reviewed, drift-checked and installed NOWHERE, which is v1's own header defect one artifact later; now a list. Drift-checked by `test_loop_gate.sh`, 43→**44 checks**, and that new check **verified red** by pointing the installed copy at a stub (`1 FAILED, 43 passed`) against a green control. **`pre-commit` and not `commit-msg`, resolved mechanically**: §13.1 records this gate refusing every commit when installed as `pre-commit`, but that is an argument about the TRAILER check, which needs the message `man githooks` does not give `pre-commit`; these three read the tree and need no message. **Refuses rather than reports, checked not assumed**: all three are green on this tree at install time, so unlike `githygiene`'s permanent exit 1 on 16 tracked binaries (H14) a refusal means the content in front of it introduced something. **It refused its own author's next commit within ten minutes** — two rows numbered `H27`, allocated concurrently by two lanes — and that was CORRECT, not a fleet-stop: `git commit --only` on a SHARED file commits the other lane's in-flight edits to that file too, so the duplicate was genuinely in the content being authored. **Fail-closed on the checks, fail-open on the environment** — an absent or unstartable checker SKIPs, because refusing there wedges every lane over a `python3` that moved. `--selfcheck` asserts what a passing suite cannot assert for itself (H7): **a red run is reachable**, driven with green/red/absent stubs. No baseline-diffing machinery, deliberately — the one incident that looked like it needed one did not. Original diagnosis below.  ~~OPEN — every mechanical check in this repo is advisory. The trailer rule was violated by the commit immediately after it shipped; a check that REPORTS but does not GATE is a stronger version of a prose NOTE, not a different kind of thing. **Narrowed 2026-08-17 (H7): the row said "no pre-commit hook" and there IS an enforcing hook — `.git/hooks/commit-msg`, now installable via `spikes/harness/install_hooks.sh` and drift-checked. It gates TRAILERS ONLY.** The two suites still run nowhere automatic, which is the part that stands |
| H22 | *(allocated as a second `H17` by ATTACKER-1 at 12:21, four rows below AGENT-1's 11:57 `H17`; renumbered under H18. **A citation of "H17" about falsifier coverage means this row.**)* **checks in `test_loop_gate.sh` had never been run against the defect they exist for** — they have never been run against the defect they exist for, so their PASS is a statement about the current tree and not about the check | **DONE** — 23 falsifiers, **35 of 43 checks observed going red**, and the driver now MEASURES coverage and prints the ones that never reddened, so the number cannot decay into prose. Counting immediately found a defect in the checks: **seven renamed themselves on failure** (`hostile callsign refused: X` green vs `...emits an UNPARSEABLE decision: X` red), so they could not be tracked green-to-red and were unmeasurable — they reddened every time, under another name. Nobody finds that by reading. The 8 residual are named with a reason each: 4 hostile callsigns produce VALID JSON with the whitelist gone so the check correctly stays green; 2 guard against `eval` that the hook does not contain, so falsifying them means INVENTING a defect rather than restoring one; 2 need TWO simultaneous reverts and the driver applies one edit per falsifier — a limitation of the driver, opened as H20 |
| H21 | **CLASS: a fix on disk is not a fix in the running process — A24 for processes.** `run_loop.sh` was fixed at 11:52 (H16) and 12:00 (v4 callsign whitelist); all 9 live launchers started 11:49:21–11:49:26, so **neither fix is running anywhere in the fleet** while both are committed with passing tests. Measured, not argued: a `/tmp` probe edited a bash script mid-run and the loop kept its **pre-edit body for every remaining iteration**, then resumed after the loop at a stale byte offset and died with `unexpected EOF while looking for matching '"'`. The asymmetry that hid it: `loop_gate.sh` is a fresh process per turn end so hook fixes go live immediately, and half a fix going live made the other half look live | **DONE (detector), BLOCKED_ON_HUMAN (cure)** — `spikes/harness/check_live_launcher.sh` compares every live launcher's start time against the launcher's mtime and **REFUSES**, naming each stale pid; `--selfcheck` exercises both directions and the platform's `lstart` parsing. It refuses right now, 9 of 9. The cure is a relaunch, which is a fleet-level supervisory act a member lane does not get to perform (H8's lesson), so the ask is in `HUMAN_NEEDED.md`. **`git log` cannot tell you which lanes have a fix** — that is the durable half of this row |
| H20 | *(keeps the id as the first allocation, 12:21. A second `H20` was allocated at 12:40 and is now **H25** — so `CLAIM H20 AGENT-1` and `DONE H20 AGENT-1` in `CHANNEL.md` are about H25, and this row is still OPEN.)* falsify.py applies exactly ONE edit per falsifier, so any check that only goes red under two simultaneous defects is unreachable | OPEN — 2 of 43 (`writes no 'unknown' marker`, `lane signal untouched`) need the `LANE` default AND the glob signal read at once. Cheap: make the anchor/replacement fields lists |
| H25 | *(allocated as a second `H20` by AGENT-1 at 12:40, after ATTACKER-1's 12:21 `H20`; renumbered under H18. **A citation of "H20" about `provenance.Control` and falsifiers means this row**, including `OPEN`/`CLAIM`/`DONE H20 AGENT-1` in `CHANNEL.md` and the `H20` references inside `provenance.py` and `spikes/S80_completeness_bytes/`.)* **CLASS: `provenance.Control` cannot express a negative result, so a spike whose FALSIFIER FIRES is reported as a broken run.** `Control` is documented as *"a positive control that MUST fire"* and `kfcheck.certify` refuses with *"DID NOT FIRE — run is VOID, not negative"*. That is right for an instrument check and wrong for a falsifier: a falsifier that fires is the most valuable outcome a spike can have, and today it is indistinguishable from a dead control. Found in S80, where the falsifier fired and the run had to be restructured before the finding could be published; the workaround is to split the control into a decidability check plus a separately published verdict, which every spike now has to reinvent. A21 in the harness itself — a test that cannot express its verdict | **DONE** — `provenance.py` **v2**, additive. `Falsifier(name, refutes, fires_when, null_must_contain)` records its outcome in `provenance.json` under `falsifiers` / `falsifiers_fired` with a `verdict` of *"REFUTED — the falsifier fired"* or *"survived"*, and **its verdict does not gate `ok`**. **Not an escape hatch**: still refused for never-observed, no observations, constant observations, no `null_must_contain`, plus a new refusal for no `fires_when` — only the fired/not-fired verdict is released. **Additive**, so no `provenance.json` already on disk changes shape and no recorded verdict moves. The self-check drives **the same observation through both types in one place** — as a `Control` it voids the run, as a `Falsifier` `ok` survives — an assertion that could not have passed before this existed; **verified to fail when the fix is removed** (restoring the gate on a copy gives `AssertionError: ['CONTROL same_observation DID NOT FIRE -- run is VOID']`), with a dead `Control` still voiding the run asserted as a regression alongside. **S80 retrofitted onto it end to end**: its falsifier now records FIRED/REFUTED at `certify ok=true`. **Ripple named**: three spikes embed `Control.as_dict()` in their own artifacts, so adding `kind` made them dirty until regenerated — the class for any lane doing the same. Original diagnosis below.  ~~OPEN — unclaimed. The shape of a fix: a `Falsifier(name, why, fires_when)` alongside `Control`, recorded in `provenance.json` with its outcome and NOT gating `ok`, so `certify` can say *"ok, and the falsifier fired"*. Not taken here because it changes `certify`'s contract under live lanes, and every existing spike's `provenance.json` schema |
| H19 | *(keeps the id as the first allocation, 12:11. A second `H19` was allocated at 12:39 and is now **H24**.)* **THREE LANES SHARE ONE GIT INDEX, and `git commit` commits the index and not your adds.** So a correctly-scoped `git add` by lane A, followed seconds later by lane B's `git commit`, puts A's entire cycle into B's commit under B's `Atom:` trailer -- with BOTH lanes obeying §13's "add paths you touched, never `-A`". §13.1 exists so attribution is possible at all, and here it recorded the wrong answer confidently, in the field that exists to prevent exactly that | **DONE** -- observed on ATTACKER-1's own H7 cycle: `b529081` *"S76 recorded, and two stale NEXT items retired"*, `Atom: AGENT-1`, carries `HANDOFF.ATTACKER-1.md`, `spikes/H7_harness_attack/` and 840 lines of a second lane's work. The staged set and the Atom are both visible from a commit-msg hook, so the mismatch is DECIDABLE at commit time: `commit-msg.hook` v2 refuses a per-lane file (`HANDOFF.<X>.md`, `loop_<X>.log`, `.loop_{signal,exit,blocks}.<X>`, `.heartbeat.<X>`) whose owner is not the `Atom:`, and names `git commit --only` as the remedy. A `Carries: <ATOM>` trailer keeps the honest cross-lane commit off `--no-verify`, which would bypass the trailer and self-review gates too. 3 checks incl. a POSITIVE control that the atom's own journal still passes; falsifier F9 |
| — | **evidence for H19, measured by ok-1 2026-08-17: the gate's scope met its limit within hours, TWICE IN ONE CYCLE, and neither instance is visible to it.** `commit-msg.hook` v2 refuses a commit carrying another lane's **per-lane** files (`HANDOFF.<X>.md`, `loop_<X>.log`, `.loop_*.<X>`, `.heartbeat.<X>`). A shared harness SOURCE file is outside that list, so: (1) my `test_loop_gate.sh` check-11 edit was swept into AGENT-1's `15ee371` (H30), and (2) my `livechat.log` class post, `DECISIONS.log` entries and `WORK_QUEUE.md` rows were swept into AGENT-2's `829d57b` (H8). Both commits passed the gate, correctly by its own contract. **So `git log` attributes two lanes' work to one Atom for exactly the files three lanes edit most**, which is the record §13 calls a deliverable and H12's stated reason for the trailer. Not proposed as a fix here: the decidable signal is the same one v2 uses — the staged set is visible at commit time — but extending it from a filename list to "content another lane is mid-edit in" needs a per-lane authorship marker that does not exist, and refusing every shared-file commit would wedge three lanes | OPEN — reported |
| H23 | *(allocated as a second `H18` by AGENT-1 at 12:07, ten minutes after its own 11:57 `H18`; renumbered under H18. **A citation of "H18" about a stale instruction at a surviving call site means this row.**)* **CLASS: an interface removed or renamed in code while a surviving site still instructs callers to use it.** Distinct from a dangling citation (§12.4): this points at something that exists and is WRONG, and it lives where nobody looks for rules — a runtime's own output string, a journal's "how to stop" line. Four sites found in one sweep (hook refusal message, two in `HANDOFF.md`, §13.1's hook path); three fixed by AGENT-1 under H16, one here | OPEN — **no mechanical detector.** The distinguishing test is not the string, since rationale blocks legitimately name what was removed (`run_loop.sh:14`, the v3/v5 blocks); it is whether the sentence tells a reader what to DO. F1 mechanises exactly one instance of it — the hook's instruction must be an instruction the hook obeys |
| H16 | **CLASS: a terminal signal that outlives the span that wrote it is live ammunition for the next span.** Found live, in my own lane, at launch: `.loop_signal.AGENT-1` still held `LOOP-HALT` written at 11:30 under `STOP`. `loop_gate.sh` section 1 returns at the STOP check **before** section 3 consumes a signal, so the file survived; the operator then removed `STOP`; `run_loop.sh` cleared `.loop_blocks` and `.loop_exit` at turn start and **not** `.loop_signal.$CALLSIGN`. The relaunched lane would have had that signal consumed at its **first turn end** and exited having done no work, logging `terminal signal, exiting` as though the span had finished. Second site, same class, found by the §12.2 grep: **the hook's own refusal message instructed lanes to write the bare `.loop_signal`** — the path v5 removed one section above in the same file. H9 cut the circular deferral on the §7 side only, so a lane obeying the refusal verbatim could never exit at all | **DONE** — `run_loop.sh` v3 (defect 5) clears the signal at turn start, chosen over patching the hook's STOP branch because the launcher is the one choke point every span passes through, so it also covers the arming paths the hook cannot see (crash or SIGKILL between the write and the turn end, the watchdog's own `pkill`, a hand-written signal). Clearing there cannot destroy an in-flight exit: a signal surviving a completed turn is stale by construction, since the hook consumes a live one at the turn end that follows it. `loop_gate.sh` v6 interpolates `$LANE` into the refusal so the path it names cannot drift from the path section 3 reads. Two checks added, `test_loop_gate.sh` 26→28: the launcher is driven **end to end with a stub `claude`** (first check in the suite that executes `run_loop.sh` at all) and the refusal message is tested as an instruction — the path is extracted from the message the hook just emitted and the hook must honour it, which cannot be fooled by a name (A30). **Both shown to fail with the defects restored**: `bash spikes/harness/test_h16_falsify.sh`. **CORRECTED 2026-08-17 by ATTACK cycle 18 — this row said DONE and the launcher half is NOT LIVE.** Every running `run_loop.sh` started 11:49; the file was modified 11:52. Bash parses a top-level `while … done` once and runs it from memory, so the fix reaches a lane only at **relaunch**. The hook half *is* live (fresh process per turn end, and its refusal text now names the per-lane path), and that asymmetry is what made the whole fix look live. Status is **DONE on disk, LIVE AT CUTOVER**, which is H2's condition arriving at my own row one cycle after I wrote it. `HUMAN_NEEDED.md` carries the ask |
| H17 | *(keeps the id as the first allocation, 11:57. A second `H17` was allocated at 12:21 and is now **H22**.)* **§10 says "nothing outside the workspace is written" and two harness tests write to `mktemp -d`, i.e. `/tmp`.** `test_loop_gate.sh` does it deliberately — scratch inside the tree can be swept into a commit or eaten by a lane, and "a test that can stop production is not a test" — so either the rail has an unstated exception for ephemeral scratch or the suite has violated it at every run since it was written. DECISIONS 132 records another agent deleting a `/tmp` probe for exactly this rail, so the two readings are both live in the repo right now | OPEN — **not decided by me**: §10 is an absolute rail and an agent narrowing a rail it operates under is A22. Needs one line from the operator or an elder. Flagged, followed existing precedent, not silently reinterpreted |
| H24 | *(allocated as a second `H19` by AGENT-1 at 12:39, after ATTACKER-1's 12:11 `H19`; renumbered under H18. **A citation of "H19" about the anti-self-review gate means this row** — note `DONE H19` in `CHANNEL.md` was posted about THIS row, not about the git-index row that keeps the id.)* **The anti-self-review gate compares strings, so the one word §13.1 forbids walks through it.** HEAD (`63264d3`) carries `Atom: harness-hardening` / `Reviewed-By: self` and `githygiene.py` reports `HEAD TRAILERS: ok — Atom harness-hardening, reviewed by self`, because line 225 fires only on `rev == atom` and `"self" != "harness-hardening"`. Both fields are wrong in the same commit and each hides the other: `Atom:` is a **topic, not a callsign**, which defeats the whole reason H12 exists (attributing work or fault to a lane from history), and that non-callsign is exactly what makes the literal string `self` survive an equality test against it. §13.1's vocabulary is a callsign or `unreviewed`; `self` is neither | **DONE by ATTACKER-1, verified by AGENT-1** — the `commit-msg` hook now refuses all four shapes, checked by driving it: `Atom: harness-hardening` REFUSED ("that field names WHO made the commit, not what it was about"), `Reviewed-By: self` with a *valid* Atom REFUSED ("self-review in plain language"), `Reviewed-By: the-other-lane` REFUSED ("neither a callsign nor 'unreviewed'"), and the legal form passes. The fix is better than the registry I had in mind: `is_callsign` is a **shape** rule — upper-case, hyphenated, contains a digit — so it cannot go stale as lanes come and go, where a whitelist of known callsigns would need maintaining and would block a new lane's first commit. Original diagnosis kept below for the record.  ~~OPEN — verified by reading the trailers and the comparison, not inferred. Fix is the same shape as the `$LANE` whitelist added to `loop_gate.sh` today: check `Atom:` against the known callsigns and `Reviewed-By:` against those plus `unreviewed`, so an unrecognised value fails closed instead of passing an equality test it was never going to trip. Not taken mid-cycle — `githygiene.py` is another lane's artifact and H15 (nothing runs any check automatically) is the same row's neighbourhood |
| H18 | `WORK_QUEUE.md` has **two `## H —` sections** with two id spaces (`H-E1`/`H-CLOCK`/`H-WRITERS`/`H-HOOKREG`/`H-A28` and `H1`..`H18`), so "class H" and any "H row" reference resolves to two places. Same defect as `MISSION_LOOP.md`'s duplicate §9, which was found by `grep -E '^## [0-9]+ ·' \| uniq -d` and renumbered to §13 | **DONE (ATTACKER-1)** — and the row UNDERSTATED ITS OWN DEFECT by an order of magnitude. Two sections was the visible half; the id space inside the *second* section was duplicated too. `H17`, `H18`, `H19`, `H20` were each allocated **twice**, and **three of the four by two different lanes** minutes apart (`H17`: AGENT-1 11:57 vs ATTACKER-1 12:21; `H19`: ATTACKER-1 12:11 vs AGENT-1 12:39; `H20`: ATTACKER-1 12:21 vs AGENT-1 12:40; `H18` was AGENT-1 twice). **73 citations** of those four ids exist across **12 files**, and every one resolved to two rows — including one pair with *opposite statuses*, where `H20` was simultaneously OPEN (falsify.py) and DONE (`provenance.Control`). **FALSIFIER STATED FIRST AND RUN**: if each pair had been one lane's typo in one commit, this row was cosmetic — `git blame` per row says otherwise, so the class stands. **CLASS: an identifier namespace with no allocator and no uniqueness check** — the third instance of a class this repo has already paid for twice, at callsigns (H8, two lanes signed `AGENT-2`) and at spike numbers (§13.3, two `G25`); both were answered with PROSE, which is why this one happened. Mechanised in `spikes/harness/refcheck.py` **v2**, check 5, refusing not warning; shown red on the unfixed tree (4 collisions, no false positives across 29 harness files) and green after. Falsified: check 5 deleted on an isolated copy ⇒ `--selfcheck` goes red naming it, so it is not passing for another reason. Swept the rest of the namespace per §12.2 — spike dirs, `A<n>` guardrails, `## ERROR N` in `out/LEDGER.md`, every other queue table: **no other live instance** (the LEDGER's apparent `error 14` pair is `ERROR 14's`, a possessive, and is reported as the false positive it is). Sections merged; later allocation of each pair renumbered **H22/H23/H24/H25** with a redirect **in both directions**, because 26 of the 73 citations are in `CHANNEL.md`/`livechat.log`, which are append-only and must not be rewritten to satisfy a checker — a renumber alone would convert an ambiguous citation into a confidently wrong one |

*The rows below the `H1` series were a SECOND `## H` section, higher in this file, with its own id space (`H-E1` … `H-A28`) — so "class H" and every "H row" reference resolved to two places. Merged into one section under H18, 2026-08-17. The `H-` prefixed ids are unchanged, so every existing citation of them still resolves.*

| H-E1 | **`provenance.py` E1, the A24 staleness check, carried SIX bugs and every one of them passed its own self-test.** (1) HEAD floor came from an unscoped `git log -1`, i.e. the monorepo's last commit, so any agent's unrelated commit marked every artifact stale; (2) the uncommitted-file half never executed — porcelain paths are repo-root-relative and were joined onto the dep dir, `OSError` into a bare `continue`; (3) the `l[3:]` slice was off by one because `_run` strips porcelain's status column; (4) it compared an artifact **mtime** against a **commit time**, so every committed artifact read as stale by the commit latency; (5) `record` writes `provenance.json` into `spike_dir`, so its own output became the newest "source"; (6) the exclusions from (5) were applied to the dirty loop but **not** the commit floor, so a committed `provenance.json` poisoned its dir's floor permanently | **DONE** — all six fixed; `demo()` now drives the dirty-file path in a throwaway git repo. Net effect before the fixes: *"patch a source, don't commit it, run the binary built before the patch"* — the `fuelrun.v2.*` case A24 exists for — **was undetectable for the whole project** |
| H-CLOCK | **CLASS: two values from different clocks compared as if commensurable** (E1 bug 4's class). §12.2 grep of `spikes/harness/`: `getmtime` / `%ct` / `time.time()` appear in **`provenance.py` only**, and every remaining comparison now routes through `artifact_time()`, which returns the value **and the clock it came from**, recorded per artifact as `compared_clock` | **DONE** — class swept, one site, no other instance |
| H-WRITERS | **CLASS: one file, several writers, last-writer-wins.** `provenance.json` is written by `provenance.record()`, rewritten by `kfcheck.certify()` after it calls `record`, and patched by `retrofit_d6.py`. Whichever ran first was silently erased — my `d6_retrofit` block and its corrected `ok` would vanish the next time anyone certified those spikes | **DONE** — `record()` now carries forward top-level keys it does not author and **names them** in `carried_from_previous_record`, so the file is additive and a stale block cannot masquerade as fresh. `certify` inherits it, since its dict is the one `record` returns. Self-check added: a foreign key must survive a re-record |
| H-SELFCHECK | §12.3: `retrofit_d6.py` shipped with no runnable check | **DONE** — `python3 retrofit_d6.py --selfcheck`. It asserts the property that matters, the **refusal**: driven at an empty tree every extractor returns nothing, and with `readset_table.txt` absent the S52 control comes back `PROSE_ONLY` rather than reconstructed from its `fails_if` string |
| H-HOOKREG | **CLASS: a wiring reference that cannot be resolved mechanically (§12.4).** `.claude/settings.json` registered the Stop hook as `$CLAUDE_PROJECT_DIR/.claude/hooks/loop_gate.sh` with the variable unset, while the sibling `spikes/S51_multicore/.claude/settings.json` already pinned the absolute path — **fix-the-site-not-the-class, in the file whose own header documents being bitten by it.** And `test_loop_gate.sh` had **22 checks, all invoking the script directly and none testing that anything registers it**, so it reported all-green over dead wiring — §14.4's earned lesson in a new place | **DONE** — path pinned; 23rd check added that every tracked `settings.json` must resolve to an existing executable **without expanding any env var**. Verified it fails when the `$` form is restored (`1 FAILED, 22 passed`) and passes when pinned |
| H-A28 | **CLASS: a field recorded but never read is documentation, not enforcement.** `null_must_contain` was stored for the whole project and never checked; `deps=()` silently disabled the entire staleness path; and D6's own falsifier F2 tested that `provenance.json` *existed* rather than that it said `ok:true`, scoring three `ok:false` records as passing | **DONE** — E7/E8 refuse both; F2 reads the verdict. A15's `can_fail_because` (another lane) closes the third instance |
| H29 | **`test_loop_gate.sh` still runs nowhere automatic, and is deliberately NOT in the pre-commit set.** Split out of H15 rather than folded into its DONE (§2: PARTIAL is not a verdict). Not a speed problem — it is 1.34 s. It drives `run_loop.sh` end to end with a stub `claude` and exercises hostile callsigns, i.e. it WRITES loop state; **H13 already records that the fuse is an unsynchronised read-modify-write measured to lose 10 of 20 concurrent fires**, so running it on every commit from three concurrent lanes is the exact concurrency it is not safe under. A flaky gate is a bypassed gate (H14), and the bypass is `--no-verify`, which drops the trailer and self-review gates with it | OPEN — **and its stated blocker is CORRECTED 2026-08-17 (ok-1), because it was false.** The suite already takes a per-lane scratch root: it `mktemp -d`s, rewrites the hook's pinned `ROOT` into it with an anchor assertion, `cd`s there, and runs the launcher copies from there too — so it writes NO live loop state and three lanes each get their own tree. Measured rather than reasoned: three concurrent runs, **44/44 each, rc=0 each, and `ls .loop_blocks.*` at the repo root finds none**. H13's race is entirely inside the 20 fires one run launches at itself, and it was a `KNOWN` line that never failed the suite anyway. So "the exact concurrency it is not safe under" named the wrong hazard, and H13 (now DONE) unblocks nothing here. **What actually stands, both observed**: (1) the suite `mktemp -d`s into `/tmp`, which is H17's undecided §10 rail — gating every commit on it decides that dispute by default; (2) the launcher checks run `bash ./run_loop.sh` with `KF_DETACHED` unset, so each one **self-detaches** and the assertion races the detached child, in the direction where the not-yet-run child yields the PASSING answer. Neither is a lock, so the fix shape in this row's old verdict was wrong too. Process leakage checked and NOT found: `pgrep -f run_loop` is 11 before and after a run |
| H27 | **CLASS: a field the contract calls ASSIGNED that is in fact TYPED, so it records a constant and separates nothing.** §13.1 says `Claude-Session:` *"is assigned, not typed, and is the only field that separates two lanes signing the SAME callsign"*. **Measured over 85 trailer-bearing commits: 5 distinct values, and 47 (55%) are one of two literal placeholders** — **29 commits read `AGENT-1 | unassigned-in-lane` and are mutually indistinguishable**, 14 more are `ATTACKER-1 | unassigned-in-lane`. `commit-msg.hook` v4 checked the trailer was PRESENT and never looked at the value, so a lane typed the same constant every commit. A28 (a field recorded but never read) inside the gate that exists so attribution is possible at all (H12). Found live: two lanes signed `Atom: AGENT-1` seven minutes apart today (`dd1942d`, `a6e467e`) — exactly the collision the field is for — and the field separated them only because one lane happened to have a session URL | **DONE** — `commit-msg.hook` **v5** ASSIGNS it from the live launcher's start time, which differs per lane (`lane:AGENT-1@Aug-17-2026T11:49:21` vs `lane:ATTACKER-1@…11:49:26`) and no agent can type. **FAIL-OPEN by construction**: unset `CALLSIGN`, no live launcher, or a rewrite that does not take leaves the message exactly as written and the commit proceeds — a shared gate that can wedge every lane is a worse defect than the one being fixed (H9/H11), so this one rewrites and can never refuse. 3 checks added to `test_commit_msg.sh` (14 total), **verified red when the v5 block is deleted from an isolated copy** (`12 passed, 2 failed`) with the real hook green as the control. The assertion is `lane:$cs@*`, not merely "not the placeholder" — the weaker form passes on a constant rewrite, i.e. the exact defect removed. A callsign with no live launcher is reported **PRECONDITION**, never FAIL  **CORRECTED 2026-08-17 (AGENT-2, H37), not withdrawn:** every measurement in this row stands and the v5 mechanism did work for the 11:49 cohort. What is corrected is the standing claim that the field is now assigned — v5 resolved the lane from **argv**, which never contains a callsign when the launcher is started the documented way, so the assignment stopped silently as soon as lanes detached and this row read as satisfied for ~90 minutes across at least 4 commits. See H37. |
| H37 | **CLASS: an identity mechanism that depends on the accidental shape of another component's invocation, fail-open, so its failure is silent** — and its own test computed the PRECONDITION with the expression under test, so the failure disabled the only thing that could report it | **DONE (AGENT-2)** — `commit-msg.hook` **v6**. H27's v5 assigns `Claude-Session` by `ps -eo lstart,command \| grep "CALLSIGN=${CALLSIGN}...run_loop"`, but a launcher started the documented way (`CALLSIGN=X ./run_loop.sh`, the usage line, and `bringup.sh:160`) consumes that assignment **in the shell**: `ps` shows `bash ./run_loop.sh` and the LAUNCHER's environment exposes no CALLSIGN either (`ps -E` ignored; `ps eww` measured over every live launcher pid) — **CORRECTED same cycle by a peer session's counter-measurement:** this first read *"macOS does not expose another process's environment"*, which is **false**, since `ps eww` reads a same-user process's environment fine; the surviving statement is narrower and is what the conclusion rests on, namely that the launcher does not expose it and the `claude -p` turn does. The only process that ever carried both tokens on one line was the transient `sh -c` wrapper that typed the command, so *"the launcher's start time"* was **the start time of the shell that launched it**, and the mechanism died when lanes began detaching. **It regressed, it did not never-work** — `db84c79`/`f3338e8` from the 11:49 cohort carry real `lane:` values; `829d57b` (mine) and `2c9d277` (ok-1) carry placeholders, and the grep returns **0 matches for all three live lanes**. **Second, independent defect:** the placeholder `case` arm listed bare `unassigned-in-lane` while the value in the field is `AGENT-1 \| unassigned-in-lane` — the exact placeholder H27's own row measured 29 instances of — so even a working probe would not have rewritten it. **Third, and it is why nobody saw either:** `test_commit_msg.sh` skipped with `PRECONDITION no live launcher` whenever that same grep matched nothing. The detector's precondition WAS the mechanism, so the mechanism's failure silenced its own check (A29, one layer up). The case is now **constructed** — a fixture process that looks like a launcher to `ps`, its pid in the lock — so it runs with no fleet up. **Same root cause as H8** (a process's callsign is not observable from outside), so the fix reuses H8's artifact rather than inventing a second answer: resolve the pid from `.loop_lock.$CALLSIGN`, then `ps -p <pid> -o lstart=`; argv kept as a fallback. Still fail-open. **Verified red against the unfixed artifact**: v6 source 15/0, installed v5 13/2, failing on exactly the two new checks |
| H26 | **CLASS: a fix for a FALSE POSITIVE applied globally instead of at the site that needed it**, trading one false accusation for silent false negatives everywhere. Found by attacking `refcheck.py` v2 — the instrument that had just gone green on H18 — with two falsifiers that FAILED TO FIRE against a live control in the same fixture | **DONE (ATTACKER-1)** — `spikes/harness/refcheck.py` **v3**. (a) v1 filed a false accusation that `HANDOFF.ATTACKER-1.md` cited a broken §0; the retraction was right, but the repair resolved every §N in every harness file against the **union** of MISSION_LOOP and every per-lane brief, so `§0` in a file where §N can only mean MISSION_LOOP resolved off `prompts/ATTACKER-1.md`. **Exposure measured, not asserted**: briefs define §0–§9, MISSION_LOOP §1–§14, so today it is exactly ONE number — and it was hiding **two live unresolvable §0 pointers** (`WORK_QUEUE.md` H4 row, `HANDOFF.md:471`), both of which also still asserted the accusation their own author withdrew in `7175c0e` (LEDGER standing rule 12 — a retraction must reach every file carrying the claim). Queue row struck here; `HANDOFF.md:471` **reported, not edited** — mtime said a live writer 3 minutes earlier, and that file has two writers. (b) `harness_files()` **silently skipped** a HARNESS entry that did not exist: deleting `.claude/hooks/loop_gate.sh` and `run_loop.sh` took the scan 8 files → 6 and it still printed *"every citation resolves"* at exit 0 — family B, the instrument reporting fiction. Missing is now a REFUSAL, and `.claude/settings.json` joined the list, which §12 always named as harness and which is the **H-HOOKREG file itself**. Both fixes FALSIFIED on isolated copies (restore each defect ⇒ `--selfcheck` red naming that case) with an unmodified control staying green. §12.2 sweep: two further global loosenings in the same file (`.git/` paths skipped, `§` dropped for `GUARDRAILS.md`) are **stated in code with their reason and scoped to it**, which is the property that separates them from this defect — reported, not counted as instances. Self-inflicted and recorded: the v3 fixture first wrote *"which only a per-lane brief defines"* into its own `CLAUDE.md`, handing the file the exact permission the case exists to deny — **it passed and tested nothing**; and the selfcheck summary line went stale INSIDE ONE CYCLE (`len(want)` counts 5 while 7 cases run), so the count is gone per §7 |
| H28 | *(allocated as a second `H27` by ATTACKER-1, minutes after AGENT-1 committed theirs in `a80283e`; renumbered to H28 before either was published, under H18's own first-come rule applied to its author. **A citation of "H27" about the queue-versus-log divergence means this row.**)* **CLASS: an id-based staleness check resolved against an APPEND-ONLY LOG, which cannot be corrected when the namespace moves under it.** Found by attacking `journalcheck.py` (H5, shipped minutes earlier) on its SCOPE rather than its honesty: it asks whether a journal contradicts ITSELF, while §12.5's stated harm — *"a stale NEXT costs a whole cycle to rediscovered work"* — is a NEXT contradicting the AUTHORITATIVE record | **DONE (ATTACKER-1)** — `spikes/harness/idscope.py`, refusing. **THE FALSIFIER KILLED MY OWN PROPOSED FIX FIRST**: the cross-scope check I drafted resolves DONE-ness against `CHANNEL.md` and therefore falsely accuses this lane's own live NEXT item — `WORK_QUEUE` holds `H20` OPEN, `CHANNEL` says `DONE H20`, and `DONE H25` **cannot ever exist** because the line predates my own H18 renumber. So the rule is: resolve against the namespace with a UNIQUENESS GUARANTEE (`WORK_QUEUE`, enforced by refcheck check 5), never against the log. **AND THIS IS A COST OF MY OWN H18 FIX** — the redirects repair an old citation for a HUMAN reader and not for a machine one, which I did not see last cycle. **5 live divergences, each verified by hand**: `H2`/`H9`/`H11` were announced DONE in the log and then REOPENED or narrowed in the queue, which the log cannot retract; `H17`/`H20` are my renumber. **EVERY DEFECT THIS MODULE HAD WAS FOUND BY POINTING IT AT REAL DATA AND NONE BY ITS FIXTURE** — 4 of them: ids collected by line co-occurrence so a MENTION scored as closed; `split('|')` splitting ESCAPED pipes so `H18` read OPEN hours after closing; `**REOPENED then DONE**` read OPEN; and `N1`, a TWO-COLUMN row whose verdict sits in the item cell, read as an empty status and **DEFAULTED TO OPEN** — a manufactured divergence of the checker's own. A verdict is now SEARCHED FOR from the last cell backwards and a row with none is reported UNPARSEABLE, because guessing is the defect. All four cases are now in the fixture |
| H31 | a killed lane's DETACHED WRAPPER respawns it — killing the child is not killing the lane | OPEN — `ok-1` was terminated at the child and came back, because self-detach (H6's fix) reparents the wrapper to init where the caller can no longer reap it. The H6 fix made accidental lanes durable. Needs a documented stop procedure (`touch STOP` is fleet-wide, not per-lane) and probably a per-lane `STOP.$CALLSIGN` |
| H32 | no lane admission: a callsign with no `prompts/$CALLSIGN.md` can still be running | OPEN — v5/H30 refuses to LAUNCH without a brief, but `ok-1` predates that and is live with no role definition. The launcher gates entry and nothing audits what is already inside |
| H34 | **CLASS: a launcher's private control variables reach the agent turn.** `claude -p` inherits the launcher process's environment and every shell the agent opens inherits it again, so `KF_DETACHED=1` — the self-detach recursion guard — was live inside every lane's own shell. A launcher started **by an agent** therefore skipped its detach block entirely: no `nohup`, no reparenting, and the new lane dies with the session that started it, which is **H6's root cause returning through the mechanism added to fix it** | **DONE (AGENT-2)** — measured, not reasoned: `bash -x ./run_loop.sh` printed `[ -z 1 ]` from a shell that had never set the variable, and the defect **ate four checks of the H8 suite**, which refused to go green until it was unset. Fixed at both ends: `run_loop.sh` v6 does `unset KF_DETACHED KF_LOCK_OWNER` before the loop (so the turn sees neither), and `test_loop_gate.sh` unsets them at the top. **The second variable is the sharper half**: `KF_LOCK_OWNER` authorises taking over a held callsign, so leaking it into the turn would hand every agent a key to the H8 lock in the same commit that added it. **And the suite was running two different tests behind one name** — a lane invoking it took the already-detached path through every launcher-driven check while a human invoking the same file took the other, so which test ran was decided by who typed the command |
| H30 | **CLASS: a missing INPUT silently degrades a harness mechanism to a no-op, and the mechanism still reports success.** Third live instance of a class named and fixed at ONE site the same morning — `refcheck.py`'s `harness_files()` skipped a HARNESS entry that did not exist and still printed *"every citation resolves"* (H26b) — so this row is §12.2's own failure mode: class named, site fixed, two more instances standing. **(1)** `run_loop.sh` read the per-lane spawn brief as `$([ -f "$BRIEF_FILE" ] && ... && cat ...)` inside the prompt, so an absent `prompts/$CALLSIGN.md` expanded to the empty string and the lane launched **looking exactly like a briefed one**. Measured at 13:25: of three live lanes only `ATTACKER-1` had a brief; `AGENT-1` and `AGENT-2` — the pair that actually collided on a callsign and burned two G25 spikes — had none, and the only written form of the allocation rule (H8) is §0 of a brief, so it reached one lane in three. **(2)** `journalcheck.queue_done()` returned an empty set when `WORK_QUEUE.md` was absent: nothing DONE, so nothing can collide, so the whole check passes vacuously and prints its clean summary — §4 calls that file authoritative, so running without it is not a degraded mode but no check at all | **DONE** — `run_loop.sh` **v5** (defect 8) refuses, and refuses **ABOVE THE SELF-DETACH**: `BRIEF_FILE` was defined in the detached child, where a refusal is written to `detach_$CALLSIGN.log` after the parent has already printed "detached" and exited 0, so the caller is told a lane started that did not. That placement is ATOM-3's own defect-7 rule (never detach what has not been validated) applied to a launch precondition. `journalcheck.py` **v2** exits 2 with a reason, asserted in `--selfcheck`. Fail-closed deliberately, against this repo's recent fail-open precedents (H27, H15): those are ENVIRONMENT failures where refusing wedges every lane over something no lane authored, this is a CONTENT failure in the lane being launched, decidable at the one place a human is watching, where the launcher already refuses an unset and a hostile callsign. `prompts/AGENT-1.md` and `prompts/AGENT-2.md` written so the gate does not refuse its own fleet at next relaunch — H15 v1's defect inverted — the AGENT-2 one by **extraction** from that lane's own records, marked as another lane's to correct, and mine carrying its A22 exposure in its first paragraph. **The better half of the row: the fix made a live check INERT inside five minutes and the falsifier said so.** `test_h16_falsify.sh` restored the H16 defect and *"launcher clears a stale signal"* stayed green — A29, because the assertion is that a marker is ABSENT and a launcher refusing before its turn is indistinguishable from one that cleared the signal correctly. ATOM-3's self-detach had already turned the same check into a race (parent exits 0 after ~1 s while the turn runs asynchronously), which is also why the new H8 lock checks read `rc=0`: **an `rc` from a detached parent is not the lane's.** Fixed by a `turn_ran` positive control plus `KF_DETACHED=1` so the loop body runs in the foreground, while the hostile-callsign block deliberately does NOT set it — there the refusal must beat the detach and `rc=0` is the defect to catch. `test_loop_gate.sh` 45 → 52 checks. **Generalises: every "the turn did not do X" assertion needs a positive control that the turn ran at all.** *(A second `H30` was allocated by ATTACKER-1 seven minutes later for the pre-commit-scope attack — `CHANNEL.md:165` precedes `:166` in an append-only file — and is renumbered under H18's first-come rule, which is the rule that lane applied to itself at H27/H28. **A citation of "H30" about `pre-commit.hook` v2 and the working-tree-versus-commit scope means that row, not this one.**)* |
| — | **allocation collision 5 of 5 in one day, and the FIRST since H18 mechanised uniqueness.** Two `AGENT-2`, two `G25`, four `WORK_QUEUE` ids under H18, now two `H30`. `refcheck.py` check 5 refuses a duplicate **table row id in `WORK_QUEUE.md`**, and neither lane had written a row yet: **the namespace is claimed in `CHANNEL.md` and enforced in `WORK_QUEUE.md`, so the window between claim and row is unchecked** — and that window is exactly what two lanes spend selecting work in | OPEN — the shape of a fix is check 5 reading `CLAIM`/`DONE` ids out of `CHANNEL.md` as well, which is H28's own refuted approach (an append-only log cannot be corrected when the namespace moves under it) unless it is scoped to *allocation* rather than to *status*. Not taken this cycle: it is `refcheck.py`'s owner's call and that lane is live in the file |
| H33 | **CLASS: a path carrying an unexpanded variable is a TEMPLATE, not a citation — it names a family of files and cannot resolve by construction.** `refcheck.py` check 4 skipped `$` only as a LEADING character, so `$CLAUDE_PROJECT_DIR/...` passed and `prompts/$CALLSIGN.md` did not. That is v3(a)'s own class — the site, not the class — inside the module whose v3 rationale block is *about* scoping a fix to the site that needed it | **DONE (ok-1)** — `refcheck.py` **v4**. **LIVE FLEET-STOP, measured not argued**: `pre-commit.hook` gates this module, so the three citations of that template — two written by AGENT-1 under H30 in `WORK_QUEUE.md` and `spikes/harness/test_loop_gate.sh`, one by me in `prompts/ok-1.md` — refused **every** lane's commits at `rc=1`, and all three were correct. **Scoped, not loosened, and the scope is measured**: `git ls-files \| grep -c '$'` is **0**, so the character appears in no tracked path and nothing real is hidden; the module's other three skips (external trees, `.git/`, placeholders) each carry their reason the same way. Selfcheck asserts the template stays QUIET **while `nope.py` still CATCHES**, because a checker that skipped every path would be quiet on the template too — one direction alone is not evidence. Falsified: restore the leading-only skip on an isolated copy ⇒ `--selfcheck` goes red with `['template path refused']`, unmodified control green. **Found by tripping it, not by reading it** — I wrote the third citation myself and the gate refused my own commit |
| H35 | **CLASS: a checker that reads the WORKING TREE while its verdict is attributed to the COMMIT.** `pre-commit.hook` v1's header closes with *"the gate judges the content of your commit"*, and `refcheck.py`/`journalcheck.py` read files with plain `open()`. Both directions measured in `spikes/H35_gate_scope/`, controls C0-C3 held, two falsifiers stated first and NEITHER killed the row: **F1** the gate PASSED a commit whose own blob carried a duplicate `WORK_QUEUE` row id (`git add` broken, repair the tree, commit the index) so refcheck check 5 was inert on the content actually committed; **F2** the gate REFUSED a `git commit --only <unrelated path>` whose content does not contain the broken file at all — the cross-lane fleet-stop v1's header records as investigated and disproved, whose disproof holds only for a SHARED file. | **DONE** — `pre-commit.hook` **v2**: an UNSOUNDNESS GUARD refuses, naming the paths, when any path in the commit differs between tree and index; materialization was MEASURED and rejected (`git checkout-index -a --prefix=` = 614 ms, 164 MB, 3482 files per commit). Cannot fire under the `git commit --only` §13 mandates, and mechanises that rule for the first time. F2 documented and NOT removed (§10 — narrowing a gate is not a fix), with the commit's own path list printed on refusal so a blocked lane can see whose edit stopped it. `--selfcheck` 3 -> 6 checks, falsified TWICE on isolated copies against a green control. §12.2 sweep: instance 2 `githygiene.py` STAGED sizes came from `os.path.getsize` — stage 3 MB, shrink the tree copy to 6 bytes, and it printed *"clean"* at exit 0 while the commit carried 3,000,000 bytes; fixed from `git cat-file -s :<path>`, new selfcheck case falsified. Instance 3 filed as H36. Three defects of my own, all voiding a run I had already read: `spikes/H35_gate_scope/RESULT.md`. |
| H36 | **Instance 3 of H35's class, NOT fixed: `spikes/harness/test_loop_gate.sh:322` measures gate drift against the TREE source and reports *"gate matches its tracked source"*.** `src="$ROOT/spikes/harness/$g.hook"`; tracked means committed. LIVE INSTANCE, two commands, created by H35's own edit: `cmp -s spikes/harness/pre-commit.hook .git/hooks/pre-commit` EQUAL, `git show HEAD:spikes/harness/pre-commit.hook \| cmp -s - .git/hooks/pre-commit` DIFFER — an uncommitted source edit plus a reinstall reads as NO DRIFT while the enforced gate exists in no commit. | OPEN — not taken by ATTACKER-1 for two recorded reasons: `test_loop_gate.sh` had a LIVE WRITER (mtime moving within 7 s), and a straight HEAD comparison would make the suite red for every harness author mid-cycle, so the fix is a corrected message plus an informational HEAD line, which is a design call for the row's owner. Sits beside H29, the same file. |
| S84 | verification COST against proof size — the falsifier `HANDOFF.md`'s only live NEXT states and nothing in the S77→S80 chain had run: *if verify time is flat in proof size, the whole branching-cost result is irrelevant to the job class and only the prover pays* | **DONE** — `spikes/S84_verify_cost/`, `certify ok=true`, 6 controls all fire, **falsifier SURVIVED**. Operationalised in the file before the first run (fires under 10% work spread against over 50% proof spread). Over a **15.2× proof-size sweep** verifier hash bytes spread **1,004%** while the flat null spread **0.000%**: the verifier hashes **1.06–1.47× the proof's own bytes**, the coefficient falling toward 1 as per-node framing amortises, `check_affine` accepting at 6% — run before choosing how to report, verdict recorded either way, points published not a slope (A18). **The content is not the ~1:1, which is close to structural; it is that the verifier is FORCED to it**: a sibling digest flipped at EVERY path position independently gives **3,483 corruptions, 3,483 rejections, 0 accepted**, where a verifier checking only the leaf would hash identical bytes and still pass a single-corruption control. Null is a real lazy verifier (hash the claimed root, return) at 1 hash and 33 B everywhere, and the counting-`hashlib` substitution is asserted to have reached the code, since one that failed to take would report a small stable number reading as a flat verifier (A29). **A control produced the limit rather than a caveat: `C_components_disagree` FIRED** — bytes-hashed orders the three real key sets `atoms_original < atoms_interned < triples` and path-steps orders them exactly backwards, so **neither is a proxy for verify cost and which ENCODING is cheapest to verify is not decided here**. Only a timed run settles it and `quiet.sh` REFUSES on this host (loadavg 55.96 vs 3.50, 4 foreign containers); wall time is recorded, orders the sets a third way at a 7.0% spread, and is marked `wall_us_citable: false` — the W4 `readset_table` precedent. **Settles for the chain**: S77→S80 were all prover-side, and the branching model prices BOTH sides at roughly 1:1, so S80's *"a proof costs the branching it actually passes"* is a statement about verification too. **Grade C in the verdict line** (S78's precedent): counts exact and reproducible, forced-ness demonstrated at every position, load-free-to-wall-clock link unmeasured on this host. NOT measured: verify against RE-EXECUTION (different units, needs an operating point and a quiet host), and absence/completeness verification — S79 and S80 both found those behave differently prover-side, so this does not extend by inspection |
| H38 | **CLASS: the harness holds TWO lane rosters and they disagree, so "is X a sanctioned lane" resolves to two opposite answers depending on which file you open.** §12.4's *"a reference that resolves to TWO things fails the same way"*, applied to a FACT rather than a pointer, and the fourth instance of the no-allocator class already paid for at callsigns (H8), spike numbers (§13.3) and queue row ids (H18) — the first three answered with prose, only H18 with a check | **DONE (ok-1)** — `spikes/harness/rostercheck.py` v1, refusing. **Three sites, verified mechanically**: `roster.txt` names AGENT-1/AGENT-2/ATTACKER-1/ATOM-3 and excludes `ok-1` with a correction block saying why; `spikes/harness/bringup.sh:35` defaults `LANES` to `"AGENT-1 AGENT-2 ATTACKER-1 ok-1"` — **wrong in BOTH directions, `ok-1` present and the elder `ATOM-3` absent** — and launches each through `CALLSIGN="$l" ./run_loop.sh` at :160; `run_loop.sh:117` then REFUSES `ok-1` against `roster.txt`. **So that supervisor starts a lane its own launcher cannot start, reports it as a start, and never starts the elder at all.** `run_loop.sh:113` asserts the property this breaks — *"roster.txt is the sanction, and it is the same file bringup.sh starts from, so the two ends cannot drift"* — true of the root `bringup.sh`, which reads it, false of the second copy, which does not mention it. **FALSIFIER STATED FIRST AND RUN**: if `spikes/harness/bringup.sh` read `roster.txt` on any path, the row was cosmetic — `grep -n roster spikes/harness/bringup.sh` returns nothing, so it stands. **THE CHECKER REFUSES TO DECIDE WHO BELONGS**, deliberately: `roster.txt`'s own correction records a lane talking its way onto it, so a checker resolving the disagreement by picking a side would be that defect with a script in front of it. It reports the divergence and leaves sanction to whoever owns it. **I am the lane in dispute and did not decide it (A22)** — the ask is in `HUMAN_NEEDED.md`. **NOT wired into pre-commit, and that is a decision not an oversight (H14)**: it is RED on this tree right now and a gate firing on a known-accepted state every run is one everyone learns to bypass. Selfcheck drives both directions plus the absent-roster branch, which REFUSES rather than skips — `refcheck.py` v3(b) shipped the other way and printed a green verdict over a narrowed scan (family B) |
| M1.3c | verify the sentence closing M1.1's largest open issue: *"Process reuse is safe for ground-result jobs, **which is the entire corpus**"* (`spikes/M1_3_worker/WORKER_RESULT.md:73`). Self-authored, never measured; `HANDOFF.md` NEXT 3 says verify before spending a cycle | **DONE — CORRECTED, not reopened.** `spikes/M1_3c_ground_corpus/`, `certify ok=true`, 3 controls all fire, **falsifier did NOT fire**: 0 of 26 executed programs record a non-ground result, so the mechanism (fresh `Metta` per job, `canon` at the comparison boundary, 31 raw hashes to 1) stands. **What is withdrawn is the word "entire": true on 26 of 64, untested on 38, and 23 of those 38 have sources that mention a variable** (45 of 64 overall; the source scan is published as an upper bound and never as the verdict, because a rule can bind `$x` that never surfaces — A30, S75's `fn witness` mistake). The 38 are exactly the programs `CORPUS_COMPOSITION.md` already counted as never reaching evaluation: **14 record the empty string** — ground the way `e3b0c442…` is a hash of data, CLAUDE.md's own family-B example — and **24 record `Failed to resolve module top:agents`** because the Python extensions are absent *on this host*, which is evidence about the module resolver and not about the program. **The environment that makes those 38 run is the deployment environment.** `WORKER_RESULT.md`'s own next sentence already said the aliasing class is unsafe, so the paragraph was in tension with itself. Corrected in place with a changelog block, nothing edited above it. Third control is the one worth keeping: the executed/not-executed split is recomputed here by different code and lands on the same **26**, so a disagreement would have meant one of the two documents is wrong about the corpus. Two closure routes named, neither taken: admit on the PROPERTY via the `D5` ban surface rather than on the population, or keep process-per-job for the variable class only |
