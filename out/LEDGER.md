# CLAIMS LEDGER v2 — what is actually true, 2026-08-17

v1 was audited and **the grading scheme itself was the defect**. Rewritten.

## Grading, fixed

| grade | meaning |
|---|---|
| **A** | measured on device, and a reviewer **specifically tried to break this claim** and failed |
| **B** | measured on device; nobody has attacked *this* claim |
| **C** | laptop only |
| **D** | projected, or composed from measured parts plus arithmetic |
| **E** | read from source or an API, never measured |
| **INVALID** | **failed a plausibility or provenance check — do not cite.** Distinct from E: an E claim is merely unverified, an INVALID one is known not to support the weight placed on it. Adopted 2026-08-16 after `GUARDRAILS` E/B2 survived the entire v2 rewrite unimplemented. Taken from BOINC's `PFC_MODE_INVALID` (`sched/credit.cpp:207`), which clamps **and** poisons the sample rather than merely downgrading it |

Three rules v1 broke:
1. **Being measured *by* an attacker is not surviving one.**
2. **Being used inside an attack is not being the target of one.**
3. v1 said *"nothing at grade A has fallen."* **Circular** — if A means "attacked and survived", the A set is the survivor set by construction. Deleted. Honest version: **two claims have been specifically attacked and survived.** n=2.

## ⚠ Read before any performance number below

**Every device measurement in this workspace ran ungated.** `quiet.sh` checked
only the host until 2026-08-17; `--device` mode now exists. On introduction the
phone read **loadavg 9.26 on 8 cores** and **not charging**. S34, S50–S54, S57,
S62 and S63 were all taken without this check, and the determinism results among
them (digests) are unaffected while the **throughput** results are not
re-validated.


**S54: Android's `background` cpuset is `0-1,4-5`.** A charge-time worker gets **four cores, never a prime core**; `sched_setaffinity(cpu7)` is silently overridden. `top-app` spans 0–7, but S6's whole premise is that we are not the foreground app.

Every throughput figure here — S50's 49.8 GB/s (cpu7), S51's 115.8 (T=7), the attacker's 67.9 (T=8), the 2.018× prime NEON advantage — is from a configuration **the product cannot enter**. Upper bounds on hardware, not estimates of the deliverable.

---

## LIVE — determinism (still the asset)

| claim | grade | note |
|---|---|---|
| **`intersection-atom` returned WRONG ANSWERS, not just nondeterministic ones** | **A** | Verified: 15 runs of `(A $x B $y C)` ∩ `($y C A $x B)` gave card 5 nine times and card **3** six times. **40% of runs silently dropped set members.** Root cause is not the pointer-keyed HashMap I first blamed — `atom_to_trie_key` collapses every variable to one `Wildcard`, so index construction merged symbol `A`'s bucket under the wildcard key. Needs two variables to reproduce |
| **Both bugs fixed, 0 regressions** | **A** | `proposed/hyperon-nondeterminism/patches/`. 30/30 correct after; `cargo test -p hyperon` **319 passed 0 failed**; **S57 corpus 67 programs, 0 rows differing, 235 assertions and 29 error atoms unchanged**. Cost: intersection/subtraction become O(n·m); the trie was only an index and correctness never depended on it |
| **LLM inference is byte-deterministic within a backend** | **B** | **S62**, on the operator's own stack on the S25 Ultra. 4 runs each: CPU `-t 1/4/8` all `4e5b2619c0fb`, Hexagon NPU `81cef5aacba9`, Adreno `028aee58fa2d`. **Thread count does not change the CPU result** — llama.cpp fixes reduction order by design |
| **No two backends agree, and not subtly** | **B** | S62. Entirely different generations, not last-ULP drift — greedy sampling turns one flipped argmax into a permanently divergent sequence. **No tolerance model saves this** |
| **The equivalence class for AI inference is binary + RUNTIME ENVIRONMENT, and it is not observable from outside the device** | **A** | **S62 attacker, reproduced by me today.** Same binary, model, seed and flags on one device — only env vars differ: default `81cef5aacba9`, `GGML_HEXAGON_USE_HMX=0` → `3b3c15512896`, `MM_SELECT=1` → `01e11bb3e178`, `ARCH=v75` → `d3848a54d223`. **Seven classes inside one "HTP0" label**, 17 such vars in the `.so`. Worse, `--fit` picks the device split from **free memory at launch**, so two identical phones disagree if one has a browser open. **This also breaks S63's "source commit + compile flags" key** — these are runtime, not compile-time |
| ~~The equivalence class is the BINARY~~ | **superseded** | **S63 corrects S62.** Mac aarch64 built with Android's exact `CMAKE_C_FLAGS` gives `4e5b2619c0fb` — **byte-identical to the phone**, across two OSes, two libcs, two devices. Divergence was `-ffp-model=fast` + `+i8mm+dotprod`, not platform. **Fast-math is harmless if both sides use it.** A marketplace can pin a binary far more easily than classify hardware — and this is BOINC's own unit of account (`GUARDRAILS` B1: a "version" is binary + platform + plan class) |
| ~~Neural work needs HR keyed on BACKEND~~ | **superseded** | S62. Corrects my repeated claim that S57 lets us delete BOINC's `sched/hr.cpp`. True for **symbolic** work (cross-ISA, S57). False for **neural** — there HR is required, keyed on backend not CPU model. Mechanism is a `backend_class` job field plus a matcher constraint; BOINC's `hr_unknown_class()` fail-closed pattern applies |
| ~~Neural inference verifiable by byte comparison, no TEE and no ZK~~ | **INVALID** | The proposed mechanism was a `backend_class` job field. A backend label does not identify the computation (row above), is self-reported, and cannot be checked. **This quietly reintroduced the trust assumption the project exists to avoid.** Symbolic work is unaffected — S57 stands |
| S62's published "CPU" row was the **Adreno** hash | **B** | The documented invocation (`-t 4`, no `-dev`) offloads layers to GPU and NPU; the CPU row needs an undocumented `-dev none`. Anyone reproducing S62 from its own writeup gets the GPU answer labelled "CPU" |
| **The S62/S63 comparator fabricated AGREEMENT** | **A** | `awk '/^> /{f=1;next}'` silently dropped every *generated* line starting with `> ` — three different answers hashed identically — and an empty capture hashed to `da39a3ee5e6b` (sha1 of "") and looked self-deterministic forever. **Wrong direction of error: in a verifier this accepts a divergent replica.** Fixed in `spikes/S62_llm_backend_determinism/extract.sh`, which fails loudly on empty | S62, conditional on backend-matched replicas. This is the cheapest possible answer to the neural verification question and it needs no new cryptography |
| **Transcendental libm DIVERGES across ISAs — the no-float branch has a hole** | **B** | **S59.** 11/197 evaluations differ arm64-vs-x86-64, 14/197 macOS-libSystem-vs-bionic, **max 2 ULP**. Per-**implementation**, not per-ISA: `sin`/`cos` split on ISA but agree across libms, `atan` the reverse, `tan` fails both (9/20 on bionic). Permitted by IEEE-754 — the libms are correct, our assumption was not |
| `sqrt-math` is exact cross-platform **by specification** | **A** | 0/20 both comparisons, and IEEE-754 *requires* correctly-rounded sqrt. Guaranteed, not observed |
| `log-math` / `pow-math` clean in sample — **not cleared** | **C** | 0/60 and 0/25, but `pow` is the hardest function to round correctly and the sweep was 5×5. Untested-clean, not proven-safe |
| **Ban `sin/cos/tan/asin/acos/atan-math` from replicable jobs** | **B** | S59. Statically checkable over the transitive import closure, alongside S58's `flip`/`&rng`/`reset-random-generator`. Alternative: ship a software libm so every device runs the same implementation |
| **S57's 66/66 never tested transcendentals** | **B** | The corpus contains **zero** transcendental evaluations — the only matching file is `stdlib.metta`, where every hit is an `@doc` block producing 0 results. S57 stands for symbolic reduction and `+ - * /`; its scope read wider than it was |
| Product hot path unaffected | **B** | HDC prefilter is integer popcount (S34, digest `f4e64fb7d70b9b0c`). No transcendental in the query path. This constrains the **job class**, not the engine |
| MeTTa byte-identical **across ISAs** — aarch64 and x86-64, libSystem and bionic, two OSes — incl. evaluation order and fuel count | **B** | **S57**, on hyperon's own 67-program corpus: **fuel identical on 67/67**, results on 66/67, **360,847 genuinely-terminating steps**, **235 `assertEqual` results and 29 error atoms identical on all three platforms**. Engine is hyperon, the one we can legally ship. **Attacked and survived in narrowed form**: 560,847 included a 200k fuel cap; result hashes are low-entropy (35 distinct/67) so the discrimination is the assertions, not the hash; evaluation-order reproducibility is *not* established; floats limited to `+ - * /` (no libm, no FMA, no float formatting); x86-64 is Rosetta |
| ~~S15 proved this "across architectures"~~ | **corrected** | S15 compared aarch64-macOS to aarch64-**Android** (`S15/RESULT.md:11,16`) — **same ISA**. It proved cross-OS/cross-libc, which is real and is not what the ledger said. Three reviews missed it because nobody asked what "architecture" meant |
| **Fuel does NOT survive branching randomness** — 4.5× swing (954→4269) on one program | **B** | S58, matched control. Corrects S57. Rung 1 bisects over step counts, so an unpinned nondeterministic job is **unverifiable, not merely unbillable** |
| **A job with a correctly-bound seeded generator is bit-identical across two ISAs** | **A** | S58 as **rebuilt by an attacker after my instrument proved degenerate**: **40 seeds × 3 platforms, ~420 runs**, `random-int` *and* `random-float`, zero divergence. Floats reach the hash at full precision, so this is a genuine bit-level float comparison. My own b4 used `(= (g) …)`, which re-seeds per call — all draws returned 526 and the program never branched |
| **`fuel + raw_hash` is NOT a sound replication oracle — PATCHED, unfiled** | **A** | **The largest finding in this workspace's verification story, and it has nothing to do with randomness.** `intersection-atom` on variable-bearing args gives 3 distinct hashes in 10 runs with zero RNG — `multitrie.rs:302-364` iterates a `HashMap` **keyed by raw pointer**. Variable naming in *every* `match` is `HashMap`-order-dependent (`matcher.rs:193-756`). Fuel stays stable, so there is a **live false-negative channel for any result containing variables** |
| **Three `Display` impls leak heap addresses — PATCHED, unfiled** | **A** | `new-space` alone gives 5 distinct hashes in 5 runs, no imports. Also `RandomGenerator` (`random.rs:64`) and `FileHandle` (`fileio.rs:94`), reachable via error atoms |
| `reset-random-generator` is a third unpinnable entropy source | **B** | Re-seeds an explicitly-seeded generator from OS entropy (`random.rs:43-45`) |
| Eleven `das-*` **network** ops are enabled by default | **B** | `lib/Cargo.toml:47` `default = ["pkg_mgmt", "das"]`. `!(match &das …)` is a network query. Plus all of `fileio` |
| **No wall-clock op is exposed to MeTTa** | **B** | A genuine and useful absence — one entire class of nondeterminism is unreachable |
| Atomspace / `match` / `superpose` iteration order **is** deterministic cross-platform | **B** | Attacked and held. `hyperon-space/src/index/trie.rs` walks insertion-ordered `SmallVec`s |
| **`flip` and `&rng` must be banned — necessary but NOT sufficient, and not an "iff"** | **B** | `random.rs:186-188` — `flip` takes no generator and calls global `rand::random()`; `&rng` is `from_os_rng()` bound at module load (`:127-128`). Neither is reachable by `set-random-seed`. But the ban is a **conservative over-approximation, not an iff**: `!(if (flip) 1 1)` is deterministic 10/10. And it is nowhere near sufficient — see the address-leak and oracle rows above |
| ~~Fuel is deterministic even when output is not~~ | **superseded** | S57. `test_gnd_conv.metta` calls `(flip)`: three different result hashes across three platforms, `fuel_used = 1012` on all three. **The meter is separable from the result.** Held 30/30 runs, 18 distinct outputs. **But nothing in that program branches on the random value**, so control flow is fixed and fuel *cannot* vary — this is the trivial case. `(if (flip) (long) 0)` is untested and the billing design needs it |
| S57's harness has a **null control that fires** | **B** | The corpus supplied it. `test_gnd_conv` diverges from itself on one machine, 4 distinct hashes in 5 runs, against a control stable 5/5. Rule 4 satisfied without my having to construct it — the argument for elder corpora, concretely |
| MORK 33/33 corpus byte-identical, incl. 48 MB dump | **B** | S16. Independently *replicated*, never attacked |
| Packed popcount bit-exact vs scalar and SDOT, both machines | **A** | S34. The UB sweep hunted unsequenced modification across every device program; `kernels.c` was clean |
| Determinism across build profiles (stock / LTO / oryon-1) | **B** | S30, 12/12 cells. Missing from v1 |
| Determinism across core types | **B, with an open finding against it** | `S52_attack_s50` scored: *digest proves repeatability, not correctness*; the kernel was **rewritten** for S50 so a regression would be invisible. v1 cited the confirming attacker, omitted the scoring one. Fix = one assertion: S45's 12-row ground truth through the new kernel |
| Determinism across thread counts and split strategies | **B** | S51 |
| `mork --timing` writes nanoseconds into the hashed space | **B** | S35 |
| MORK's space is a set, hyperon's `match` a bag | **B** | S35 |

## LIVE — architecture requirements

| claim | grade | note |
|---|---|---|
| ~~W1: a witness is ~4.2 KB regardless of shard size~~ | **INVALID** | **`witness.py` modelled an engine that does not exist.** `S52/realkg.c:184` scores every bundle on every query — no skip, for any shape — so the read set is **100% of the prefilter index**. Corrected witness **1.54–12.23 MB**: 1.0–8.3× over a cold fetch, **543–4,297× a resident replica**. Also a unit error (12.8 MB is a B=8 index over 800k triples, not raw triples) and **zero working controls**. Not E — it was cited as load-bearing | Read-set commitment with Merkle range proofs. 4,224 B at a 48 KB shard, **4,416 B at 3 MB** (712×), ~3,000× extrapolated to 12.8 MB. **A non-resident verifier costs ~1.5× a resident one, not 4,500×** — S69's traffic objection dies, the verification pool becomes the whole registry, and coverage returns to S61's plateau. Ships code, seed, and 4 controls |
| ~~Witnessed verification works only where clustering aligns~~ | **INVALID** | Premise falsified by S52 on the identical corpus and clustering: measured **0.2% / 1.0% / 8.8%** store checked, against W1's 7.7% / 100% / 100%. Under S52 *two* of three shapes are cheap under `(pred,subj)`, and every clustering in its matrix leaves exactly one expensive shape. The honest statement is that **the cheaply-verifiable job class is whatever fraction of demand hits the aligned shapes, and that fraction is unmeasured** — converting an unmeasured demand split into a prerequisite is the move that killed S69 | W1: `(p s ?o)` touches 1 of 13 chunks; `(p ?s o)` and `(?p s o)` touch all 13, so their witness is worse than the shard (0.9×). **First argument for wedge #2 that does not rest on throughput** — S52 made shaping a 4.1–5.6× speedup, W1 makes it the thing that makes verification affordable |
| **W4: the prefilter's read set is 100% by construction and cannot be made sublinear without changing what it is** | **B** | `score_row` is a Hamming similarity over the whole hypervector; nearest neighbours are **not contiguous in any key order**, so there is no prefix to bound. W1 presumed a key lookup. Sublinearity needs an ANN index, which trades recall (S17: 0.97 mean, **0/100 minimum**) and voids S52's layout comparison. Measured 78.59× amplification is oracle-fitting harness outside S52's timed region. **ATTACK cycle 8 sharpens this:** S52 says *a fixed cutoff cannot reach recall 1.0*, so a deployment dropping the oracle keeps the 1× read set **at an unmeasured recall** — S52's µs figures correspond to a configuration requiring ground truth. Probabilistic spot-check was tested and fails: at k=1 altered bundle, sampling **20% of the index catches 18%** |
| **C4 quantified: 5 adversarial devices on a rare shard get wrong results accepted 72% of the time at honest pool 3, 0.5% at 100** | **B** | **Q1**, the first join of S61's fleet model with S49's envelope adjudication. Captured seats and accepted-wrong **track exactly**, because colluders under one operator echo one digest — `DuplicateSourceInMatch` rejects one *source* twice, not one *operator* twice. Honest pool must exceed ~25 before capture drops under 10% against just five adversarial devices. Both controls fire |
| **Root cause of S69/S70 stands; the FIX does not** | **B / unsupported** | The diagnosis — eligibility coupled to residency — holds. **W1 was the proposed cut and it is INVALID**, so residency coupling is *not* cut and the fleet-wide verification pool is **not** restored. C4's rare-shard attack is live again. **New open question (W4): what is the read set of the HDC prefilter, and can it be made sublinear without invalidating S52's timings?** Nothing about verification eligibility can be decided before that | The rare-shard attack exists only because the quorum pool is *devices holding the shard*. The global-seat fix died because a non-resident verifier pays 12.8 MB while being the adversary's **cheapest** device. Duty-cycle exploit is availability weaponised against a draw over the online set. Coverage-100 was invented to fatten residency pools. **Five pathologies, one coupling** |
| **D1+ — seat draw ∝ stake over an epoch-committed registry, never over the online set** | **spec** | Drawn-while-offline → timeout → penalty → stake-weighted redraw. Staying online then buys an adversary nothing but not-missing slots. Applies to every seat. MVP concession remains a trusted coordinator, now with a stated spec to emulate |
| **D3→ — economics ship as FORMULAS with shard demand a declared unmeasured input** | **spec** | No constants derived from it. Only `N·C/S ≥ R` survives, as bare feasibility. Orphan-shard forced placement priced as **availability**, not security |
| **POL — "operating point" abolished** | **spec** | Coverage is a demand-driven policy targeting S61's plateau (~5), floored only for availability. Not a security parameter |
| **Shard demand is the load-bearing unmeasured quantity, and it cannot be measured from inside this workspace** | **A** | S52's generator samples **uniformly over triples** (`realkg.c:170`) — an artefact, not a workload. The only instrument that measures real demand is a real user's query stream. **The buyer is no longer only the business risk; it is the missing measurement device**, and the security model's last free parameters wait on a design partner's logs |
| **Per-device supply is 2.83 jobs/s (1 worker) / 11.17 (4 workers), measured** | **A** | **S71 = C2 answered.** Sustained, background cpuset `taskset 33`, `job_terminating.metta` at exactly S32's 100,082 steps, zero failures, **device gate green both before and after**. Scaling 3.95× at 4 workers. Replaces `RISKS.md`'s 2.87, which was `28,726/10,000` from an INVALID projection — and was within **1.4%** of the truth by luck |
| **A single device saturates per-job settlement** | **B** | 11.17 jobs/s at 4 workers (or **8.25 at the 3-worker shape** once a coordinator is subtracted — S71 gives both, and downstream has been quoting 11.17 unconditioned). Against a ~17 jobs/s ceiling with 3× replication: **0.77 devices**. Sharpens R-NEW and strengthens the case for Merkle batching |
| **The dispute path is replaced by majority-of-quorum, not repaired** | **B** | S68 blocked bisection-to-a-committed-state; the substitution needs only byte-identical results (S57), which exist. **Deletes from the critical path:** the state commitment, bisection, the zkVM stack, the Groth16 trusted setup, and the **prover tier** (10⁵–10⁶× native, in no schema or economic model here). Cost is 3× execution — the abundant resource — and **0× additional PoV under batching**, since the quorum compare is off-chain |
| **The seal (M3.4) is superseded ONLY where the honest pool per shard exceeds ~25 — live below that** | **B** | `PORT_PLAN:86` calls it "the highest-value single fix identified by this recon." **Corrected 2026-08-17.** "A majority of three cannot be carried by two colluders" is true *per draw* and says nothing about **where the three are drawn from**. Q1 measures the gap: at honest pool 3 — what quorum-3 degenerates to when eligibility is coupled to residency and the shard is rare — **72% of wrong results are accepted**. Quorum defends against independent minority error and **scales with the eligible pool**; the seal defends against echo and is **pool-independent**. They are not substitutes in the small-pool regime. Three non-exclusive fixes: decouple eligibility from residency (diagnosis stands, fix died with W1), impose a per-shard honest-pool floor at admission, or **reinstate the seal for the rare-shard class only** — cheapest, because pool-independent by construction |
| **Quorum-3 costs 50% of the effective candidate pool — and locality shrinks that pool** | **B** | C4. Random assignment: 50% of the fleet to control 2-of-3 at even odds, **independent of N**; a 1% adversary gets 0.027% of jobs. **But wedge #2's locality-aware matcher biases assignment toward shard holders**, so an adversary holding a *rare shard* faces a far smaller effective N. The seal is superseded for the uniform case, **not for rare shards.** ~~Priced in S69~~ — **INVALID.** Breadth is **quadratic** (~`(A/k)²`), not linear. The fleet-wide replica is **backwards**: a global-only device needs no residency, so it is the adversary's cheapest. And `R` is a residency floor while the draw is over *online* devices — at honest duty 0.25, **five always-on devices beat an R=25 shard (P=0.42)**. Traffic cost is **~1,500× not 1.33×** (a global replica has no residency to amortise: 12.8 MB/job vs 2.9 KB). Former claim: one replica drawn fleet-wide collapses it by forcing 2-of-2 rather than 2-of-3: with R≥25 an adversary needs ~72% of a shard's pool for even odds and ~12% for a 1% chance. Cost is ~33% more shard traffic, against a locality gain S61 shows plateaus at ~3× |
| ~~`N·C/S ≳ 4R`, from two constraints agreeing~~ | **INVALID** | **Circular** (both "free" rows had coverage 100 and R 25, and 100=4×25 by construction; R was never varied). **Refuted by its own table** (the 236 MB row meets the floor at 1.14R). **"Agreement" manufactured** — S61 says coverage ≈5 and puts imbalance at 32× at coverage 100, so the constraints point *opposite* ways. Survives only as the feasibility bound **`N·C/S ≥ R`** | **S70.** Enforcing `R=25` costs **236 MB/device at coverage 10** (nearly 3× what a device holds) and **~0 at coverage 100**, because demand-driven caching already satisfies it. S61 independently put the locality plateau at coverage `N·C/S`. **Coverage ≈ 100 satisfies both** — first parameter here derived from two constraints agreeing rather than one |
| ~~94% of shards below R=25 at coverage 10~~ | **INVALID** | The Zipf α=0.8 fit used **predicate frequency as a proxy for shard demand** — the wrong axis. S52's own generator samples uniformly over triples (`realkg.c:170`), giving max/median **1.04**. Coverage needed spans **2R to 780R** across demand models. `min=0` is also a category error: S8 has a desktop shard host origin and M1.5 calls the phone side an LRU **cache** | S70, Zipf α=0.8 fitted to S52's FB15k-237 skew (max 15,989 vs median 373). **Minimum pool = 0** in two configurations — some shards cached by nobody, an *availability* failure neither S61 nor S69 surfaced |
| **Matcher needs two constraints Acurast does not have** | **B** | S69: minimum replication breadth **R ≥ 25**, and **one replica of every quorum drawn uniformly from the fleet** rather than the locality set. Acurast's pallet has no locality concept at all, so this is the ADAPT half of GAP row 4 |
| **The self-reported binary hash is a matching hint, not a security control** | **B** | C1. Unlike S62's `backend_class`, the hash is not the correctness argument: a device reporting an approved build and running another **produces a different result and loses 2-of-3**. Failure mode is a failed match, not an accepted wrong answer. Residual is C4, not C1 |
| **Attestation now carries independence, not correctness** | **B** | Strictly weaker than Acurast. A compromised TEE there yields a *trusted wrong answer*; here it yields *one wrong answer that loses a 2-of-3 vote*. **The property survives a broken attestation root rather than depending on one** |
| **The ban list is enforced by BUILD, not static analysis — which is undecidable here** | **A** | `python/hyperon/stdlib.py:139-140` resolves `py-atom` from a **runtime string**, so `!(py-atom math.sin)` is invisible to any import-closure analysis. But the banned ops are *registrations* — `grounded_op!(SinMathOp, "sin-math")` at `math.rs:219`, registered `:426`, **zero `cfg(feature` in the file** — so not-registered means not-reachable by any path. S63's binary-hash field already pins it. **No checker to write, and an unregistered symbol fails identically everywhere, so quorum resolves cleanly instead of splitting 1-1-1** |
| ~~The static ban list is load-bearing on settlement~~ | superseded | Majority-of-quorum degrades **silently** under an S59-class divergence: three libms could split 1-1-1, producing no majority, which reads as a failed job rather than detected nondeterminism. Prevention is at **admission**, not adjudication |
| ~~No interpreter-state commitment — dispute path blocked~~ | **descoped** | S68 stands as a measurement; it now gates a design no longer on the critical path | **S68.** `current_results()` is forgeable (S65). `Debug for RunnerState` is not reproducible: raw `Debug` gives **10 distinct digests in 10 runs**; masking addresses, sorting `Variables({…})` and masking variable ids gets to **2 in 20** and no further. Fourth contaminant unidentified. **Masking is a diagnostic, not a fix — a verifier cannot mask.** Bisection needs state at step *k*; without it a dispute can only be resolved by re-execution, the cost succinct proofs existed to avoid |
| Issue 3 is on the **critical path for settlement** | **B** | S68. Variable ids differ *in content* across runs (`"rargs" id: 8` vs `"result" id: 9`), so sorting cannot repair it. It is the one item in `proposed/hyperon-nondeterminism/` shipped as a report with **no patch** |
| The **happy path** is unaffected | **B** | Merkle-batched result commitments plus payment channels need no state commitment. Only disputes do |
| **Every graph-AI family in the elder set has an integer formulation** | **B** | `analysis/GRAPH_AI.md`. Attention (DAS Hebbian/ECAN), VSA (torchhd), symbolic+PLN (hyperon). Determinism selects a *sub-family* of graph AI rather than excluding it — and that sub-family is what an integer NPU runs fastest |
| ~~DAS's `double` ECAN fails the N-thread==1-thread oracle~~ | **INVALID** | **My harness parallelised a reduction DAS does not parallelise.** `visit_nodes` is a serial trie walk over a fixed 16-way content-keyed alphabet — order is content-determined. The oracle result is a property of my test, not of shipped DAS |
| **DAS's concurrent epochs diverge from serialised — INTER-phase, not intra** | **B** | Corrected mechanism: `HandleTrie::traverse(keep_root_locked=true)` makes each pass atomic (`HandleTrie.cc:227-233`), so there is **no torn read inside `collect_rent`** — my first claim. `spread_stimuli` runs several root-locked passes and releases root between them, so epochs interleave at phase boundaries. Frequency scales with the window: `match_serialised` 47/50 at GAP=0, 20/50 at 100, **0/50 at 2000**; the patch gives 50/50 throughout | **S67b, measured.** Faithful model of `spread_stimuli` — serial content-ordered traversal, per-node mutexes, same arithmetic. Serialised `62f3d55a…`; concurrent gives **2 distinct outcomes over 8 trials and 0/8 match serialised**. The defect is a **torn aggregate**: `total_rent` sums importance another epoch is mid-way through writing. **Fixed-point does not fix this** — it is not a rounding problem |
| ~~ECAN cannot diverge via FMA contraction~~ | **INVALID** | **Inverted.** `StimulusSpreader.cc:74-75` is `lowerbound + (range_size * arity_ratio)` — an `a + b*c` my line list omitted. clang at `-O2` with default `-ffp-contract=on` emits `fmadd` on aarch64, `mulsd`+`addsd` on baseline x86-64. Inert only while the bounds are equal (default), live after `set_parameters` |
| **Unsynchronised `unordered_map<string, HebbianNetwork*>`** | **B** | `AttentionBrokerServer.h:213`, no mutex in the class, `operator[]` insertion from concurrent handlers = rehash race. **More severe than the epoch bug** and unpatched | Every float op in the hot path is a bare multiply or bare add in its own statement (`StimulusSpreader.cc:51,67,68,76,77`) — no `a*b+c` for clang's default `-ffp-contract=on`, and `src/.bazelrc` sets no FP flags. Closes the arithmetic-half open question, negative |
| ~~DAS runs whole epochs concurrently with no epoch lock~~ | promoted E→B | `spread_stimuli` is called inline from the gRPC handler (`AttentionBrokerServer.cc:64`, async enqueue commented out), so the thread pool interleaves entire read-modify-write epochs on one shared network, per-node mutexes only. **Read from source, not measured** — demonstrating it needs two clients against a built DAS |
| A fixed-point BSP ECAN passes the N-thread==1-thread oracle | **B** | **S67**, reproduced 3×. 4,096 nodes, 12 epochs: `double` gives **four different state hashes for T=1/2/4/8**; fixed-point gives one. Anti-degeneracy guard asserts >half the nodes move and >100 distinct values. Reference implementation in `spikes/S67_ecan_fixedpoint/`. The cause is the **rate multiplication rounding plus non-associative float addition**, not integer addition — "use integers" is the wrong summary of the fix |
| ~~DAS attention broker is nondeterministic by construction~~ | **retracted** | The stated mechanism was intra-epoch reduction order, which is false — traversal is serial and content-ordered. `typedef double ImportanceType` (`HebbianNetwork.h:17`), and `total_rent += rent` accumulates over a trie walk with `WorkerThreads.cc` present. **Float summation across threads is order-dependent.** Not the S59 transcendental problem — pure accumulation order. Fix: fixed-point importance; stimulus is already `unsigned int` |
| **torchhd makes determinism a model choice** | **B** | `cgr`/`mcr` are `int64`-only; `bsc`/`map` support bool/int8/int16/int32; **`hrr`/`fhrr`/`vtb` are float-only** and `fhrr` FFTs. Our prefilter is already `MAP` at int8 — S34 bit-exact, digest `f4e64fb7d70b9b0c`. Arrived at by accident, should be chosen on purpose |
| **PLN is verifiable as written** | **B** | `c3_pln_stv` does f64 truth-value chains over 37,788 steps and matched on all three platforms — because `+ - * /` are IEEE-754 correctly rounded (S59). It breaks the moment a confidence formula uses `log`/`exp` |
| **The NPU is an integer machine — so the fast path and the deterministic path are the same path** | **B** | `executorch/backends/qualcomm/quantizer/` config space is `use_8a8w / 8a4w / 16a16w / 16a8w / 16a4w / 16a2w / 16a4w_block` — **integer activations × integer weights throughout**, with a single float option (`use_fp16a8w`). Dtypes: int32 ×68, int8 ×64, uint16 ×42, int4 ×26. **Integer accumulation is associative and exact** (`S7:9`), and S34 measured packed int kernels bit-exact on two machines. Quantized inference does not break determinism — **float inference does** |
| **"Neural inference destroys determinism" was wrong as stated** | **corrected** | True of float (S59, 2 ULP). False of the quantized integer models the NPU runs natively, and of HDC/VSA and integer graph models. The constraint is the number format, not the fact of being a network |
| **The symbolic/neural "fork" was stated at the wrong layer** | **B** | It is not two products. `pub trait Grounded` (`hyperon-atom/src/lib.rs:485`) is **user-implementable**, so any computation — including neural inference — can be a MeTTa atom. hyperon already binds external services (`exts/snet_io`), a network atomspace (`builtin_mods/das.rs`), Python (`exts/py_ops`) and agents. **The module bus exists.** The real fork is per-module *verification rung*, not per-system architecture |
| **We deleted the machinery that verifies neural work, on purpose** | **B** | `S7:9` — *"TOPLOC … verifies with an **exponent-exact / mantissa-tolerant** comparison, because GPU float accumulation order varies across hardware … We therefore keep TOPLOC's compression and **delete its tolerance model**."* That tolerance model is purpose-built for exactly the heterogeneous-float problem S59 later measured. It is MIT, in `elders/toploc`, and we discarded it on the assumption we would only ever run integer work |
| Verification rung should be chosen **per module type**, not per system | **D** | Symbolic coordination → byte comparison (S57). Neural grounded atoms → TOPLOC exponent-exact/mantissa-tolerant commitment. Untested together |
| **Stage 2 must run in-process** | **A** | S45 + S55/S56 + two attackers. Controlled pair, same session, same cores, `fork`+`execve`+`waitpid`, n=30: **7.07 ms spawned vs 0.247 ms resident = ~29×** |
| ~~"5.25 ms of the 5.66 is process creation, so MORK's own work is ~0.4 ms"~~ | **DEAD** | A rule-3 subtraction, and falsified two ways: `morkinproc` costs **more** as a process (7.12–7.33 ms) than `mork run` does (7.07) while doing strictly less work — the residual is binary size and ELF relocation, not engine work; and `mork run` on an **empty** program costs 6.98 vs 7.07 for the real one, a 0.09 ms gap against a per-sample sd of 0.45. **Process-level timing cannot resolve stage-2 work at all** |
| Stage 2 in-process = **0.25 ms** (12 processes × 3000 reps, background cpuset, sd 0.06, CV 23%, band 0.16–0.33) | **A** | Two independent attackers converged on it from separate binaries. Output byte-identical to `mork run` **and to S45's ground truth**, md5 `5befd44b…` — the rule fires: 12 facts + 1 `exec` in, 11 deduped triples + 11 derived `(Answer …)` out |
| ~~Any stage-2 engine swap is capped at ~1.06×~~ **RE-OPENED** | **D** | S44 computed this against a laptop prefilter and a numpy exact-match. Stage 2 is **~57% of the query, band 45–66%** — recomputed against S54's *own deployable shape* (T=3, 0.194 ms; I had used the T=4 cell S54 itself flags as having a free coordinator). **"Majority" is not supportable**: at the fast end of stage 2's spread against the T=3 prefilter it is 45%. What survives is decisive and weaker: **stage 2 is the same order of magnitude as the prefilter, so the 1.06× bound is dead.** Also a lower bound only — measured on 13 synthetic uniform triples and one query shape; S52 found real-KG shortlists reaching 95% of the store, and stage 2 scales with shortlist size while the prefilter does not |
| **Never size a spin-barrier pool to `nproc`** | **A** | S51: T=8 collapses 89×; S53 hit the same starvation at T=1 (14× error) one spike later. Corroborated twice, by accident |
| 4 background cores − 1 coordinator = **3 workers**, or the barrier must block | **B** | S54 |
| Custody is justified by **network fetch**, not bandwidth (~4,500 queries per 12.8 MB fetch) | **D** | S34. Retires S18's argument |

## LIVE — platform

| claim | grade | note |
|---|---|---|
| ~~Cache padding the barrier atomics changes nothing (>6%)~~ | **WITHDRAWN** | **N1d**: the positive control never fires either — 0.99–1.06× down to 5 cycles/crossing — because the forced build creates **true** sharing, not false sharing. `gen` and `left` are barrier state every core must observe, so the line bounces regardless of layout. The experiment **contains no false sharing to detect**; the null is unfalsifiable as constructed, not wrong. A valid positive control needs per-thread private data co-located with hot shared data. **Candidate corrected by N1e:** `b_dyn` is a mode flag, read-only after setup. The work-stealing counter is **`b_cursor` (`mc.c:60`)**, and the contention is not the phase story either — `dispatch()` spins on `b_left` for the *whole* work phase (`mc.c:114`) while workers `fetch_add` `b_cursor` on the same line. Continuous, concurrent, different variables: genuine false sharing. **N1e measured it and the result is UNRESOLVED** — the device changed DVFS band mid-experiment, leaving 3 control arms against 1 treatment arm in the fast regime. Balanced regime gives **4.4 pp, not the ~20 pp the imbalance suggested**. See A16 and `spikes/S51_multicore/N1e_RESULT.md` | **N1c**, after `crossbeam-utils/cache_padded.rs:94` (`align(128)` on aarch64). 23.9 µs and 2.98× both ways. ~20,000 cycles of work per barrier crossing amortises the invalidation away. **So the workspace's multi-threaded figures carry no hidden false-sharing term** — at this granularity. S51's finer-grained barrier is the untested candidate. Noise floor **5.9%**, from a control the treatment cannot affect (T=1) |
| **C3 corrected: 5.7× short at 4 workers, and the kernel is BANDWIDTH-bound** | **B** | **S72b**, the 4-worker measurement S72 admitted it was missing. 15.2× was single-core. Per-instance falls **329 → 220 GOP/s** as workers are added; scaling **2.68×** vs S71's 3.95× for independent jobs. **S34's "13.9× faster while staying compute-bound" is scoped to single-core.** My original prediction was off by 2.4×, not 6.3×. **A bandwidth bound cuts toward the NPU** — VTCM is on-chip memory — so the descope now stands on the ladder argument alone | **S72.** Predicted ~2.4×, measured **15.2×** — wrong by 6.3× because I treated the shortfall as linear in worker count when the roof does not move. **This argues against my own descope**, not for it. Exactness unchanged: all three kernels digest `f4e64fb7d70b9b0c`. First S34-family measurement taken behind a device gate |
| **Prefilter costs 23.9 µs at the deployable 3-worker shape — the Amdahl rung is measured and stronger** | **B** | **N1**, replacing the ~50 µs that inherited S18's artifact. 5.22 cycles/bundle, near-perfect scaling to T=3 (2.99×). Against S56's stage 2 the prefilter is **7.0–11.1% of the query** — a range, because S56's stage 2 is a **1.66× DVFS band (191.88–319.01 µs)** it explicitly retracted as a point value. N1b caught that the numerator was normalised (cycles/bundle) and the denominator was not. Every point in the band is well under the inherited ~17%, so **the descope's last rung is stronger, and it is a range not a number** |
| **T=4 on the background cpuset is coordinator starvation — third occurrence** | **A** | N1: 8080 µs vs 23.9 at T=3, **337× slower**. S51 found it at T=8, S53 at T=1, and the LEDGER already carried the rule *"4 background cores − 1 coordinator = 3 workers"*. **The rule was written and the code hit it anyway** |
| ~~At the deployable shard size the kernel is compute-bound again~~ | **FALSE** | **N1b isolated it**: same code, same barrier, T=3, only shard size varying — 1.60 MB scales **2.96×**, 12.8 MB scales **2.91×**. Shard size costs ~7% in cycles/bundle and **nothing in scaling**. S72b's 2.68× is **workload shape**: four processes each scanning the full store is 4× the traffic, where N1 splits one query's rows. Both measurements correct, measuring different things |
| **The NPU descope is a resource-allocation decision, not a technical conclusion** | **D** | **`analysis/NPU_DESCOPE_AUDIT.md`.** The QNN licence rung is **verified dead** — oflineAI ships zero QNN/QAIRT and declares vendor libs rather than redistributing them. The SDK/delegate rung is largely paid (ggml-hexagon is MIT and already built). The ~50 µs Amdahl rung **inherits S18's overhead artifact** by the workspace's own note. Meanwhile S72b (bandwidth-bound), B1 (VTCM premise holds at B=16) and oflineAI (700 t/s prefill, shipping) all moved the other way. **What remains is not the reason I gave** | The prefilter it was meant to accelerate costs ~50 µs, and the CPU runs it bit-exactly using `asimddp`+`i8mm` (**verified present on device**) at 13.9× scalar (S34). Measured on this silicon: NPU prefill 700 t/s vs CPU 48, but decode **18.8 vs 22 — worse**. It is a prefill/batch engine, not a per-query one |
| ~~Descoping retires the biggest grade-E claim~~ | **weakened** | Cross-vendor requantisation bit-exactness was the single largest unmeasured assumption in the architecture. Removing the NPU from the critical path removes the claim. Cheaper resolution, same risk retired |
| NPU tok/s figures are **READ**, not measured here | **E** | Sourced to `~/alex/oflineAI` (CPU 6-thread 48/22; NPU HTP0 v79 700/18.8). No NPU code has ever run in this workspace |
| `background` cpuset = `0-1,4-5` | **B** | S54, read from `/dev/cpuset` on device |
| NNAPI exposes no accelerator on SM8750 | **A** | S31; a second agent independently searched HAL/VINTF — for a negative existence claim, an independent search *is* the attack |
| Rule: **scale ≥ 2·nnz(Q)/126** — pick the scale from the cutoff, not the observed range | **B** | S31. v1 had the symptom (recall 0/8), not the rule |
| Transmit the scale as an **exact rational**, never a float | **A** | **Independently confirmed from outside this workspace**: Acurast ships `MetricInput = (PoolId, u128, u128)` → `FixedU128` as `numerator/denominator` at 260k devices (`acurast/common/src/types.rs:558-561`). First external confirmation any claim here has received. **And `hyperjob_v1.proto` still declares `quant_scale` as `double`** |
| *(original note)* | | S49: the boundary sat on .5, so float-vs-double splits two honest verifiers. `rint` in a spec is insufficient — rounding modes differ across Python/C/Go/JS |
| Pointer tagging aborts PathMap's `slim_ptrs` | **B** | S16; *used* in an attack, never targeted. v1 said A |
| Perf cluster 2 NEON ops/cycle, prime 4 (2.018×) | **B** | Attacker-produced, unattacked — **and unbankable**, prime is outside the background cpuset |
| MIT declared in MORK issue #2, file never committed | **B** | See the row below; `analysis/MORK_LICENCE_CHECK.md` |
| **MORK IS callable in-process** — `kernel/src/lib.rs` exports `pub mod space`; stage 2 in-process = **0.25 ms** (S55's "0.310 ms" was one draw at the ~80th percentile) | **B** | **S55.** v2 said the opposite at grade E and let it gate the query path. `Space::new` / `add_all_sexpr` / `metta_calculus` / `dump_all_sexpr` are all public, and an in-tree crate already uses them. **Blocker count goes 5→4, not 2→1** — licence, nightly, `/dev/shm`, and heap tagging all remain; `proposed/mork-license/README.md` said so before S55 and I contradicted my own artefact |
| ~~MORK ~31.6× faster on join work~~ | **D** | S35's two rows use **differently-bracketed timers**: MORK's internal `steps took` (excludes the 31% parse), hyperon's `run_ms` (includes it). Re-measured today: 8–9 ms vs 942–1001 ms = **~110×**, both moved >1.4× from S35. Direction robust, magnitude not quotable |
| **MORK in-process is ~10× hyperon in-process on stage 2, ~7.5× end-to-end** | **B** | **The denominator that decides anything**, and nobody had measured it until an attacker did. MORK ~0.19–0.32 ms vs hyperon ~3 ms (boot 23 ms, amortisable). S55's 18.3× used MORK-as-subprocess — a straw man nobody proposed shipping |
| **In-process MORK needs heap pointer tagging off, process-wide** | **B — the app route is SHIPPED** | Bare binary aborts without `LD_PRELOAD=libnotag.so` (exit 134, verified). App route is `android:allowNativeHeapPointerTagging="false"` — attribute present on API 36, and the operator shipped it in another APK on this phone — but **never executed here**. Costs MTE agent-wide, permanently, with no per-allocation escape (PathMap's non-slim path does not compile) |
| MORK unlicensed at HEAD — **no licence file anywhere in the tree, no `license` key in any Cargo.toml**, remote HEAD `0653b50` | **B** | Promoted from E. `analysis/MORK_LICENCE_CHECK.md`, dated, reproducible, checked against remote not a local clone |

## LIVE — magnitude survives, mechanism did not

| claim | grade | note |
|---|---|---|
| Perf→prime core migration is worth **2.26×** | **B** | v1 buried this in DEAD; only the *mechanism* died. Third un-retraction |
| A **1.43× scheduling headroom** exists | **B** | Attacker's figure; v1 over-killed the claim and recorded nothing in its place |
| Single-thread throughput flat across size (**≤0.5%**) | **B** | S50/S53. v1 listed "flat within 1.8%" as DEAD and left no LIVE row saying flatness is real |

## LIVE — shaping, oracle labelled

| claim | grade | note |
|---|---|---|
| Shaping worth **1.43×–5.64×** on a real KG | **D** | S52. v1 wrote "4.1–5.6×", dropping the 1.43. **Composite**: measured prefilter + *computed* `shortlist × per_check`; **stage 2 never executed** (S47:60); `per_check` varies 2× run-to-run |
| Store checked: shaped **0.0–1.0%**, random **5.2–57.2%** | **B** | v1 dropped the 5.2%. **Not a time proxy**: 207× fewer rows buys 4.36×, implying a ~13 µs floor and an asymptotic cap near 5.7× |
| **The cutoff is oracle-fitted** — reads ground truth; ~18 ms to produce a "127.8 µs" query | **B** | Rule 5 requires this on the row; v1 relegated it to a gap |
| Worst mis-shaping 1.45× worse than random | **D** | Same composite caveat |
| Selective-query shards should not pay for shaping (1.2–1.45×) | **D** | S48 finding 3, replicated. A surviving *negative* result, absent from v1 |
| Exactness rule is **m ≥ 2 of 3 bound slots** | **B** | S10. `(p ?x ?x)` is inexpressible → ~10% scan |
| Layout buys CPU work at fixed recall, not recall | **B** | S17 |

## LIVE — residency

| claim | grade | note |
|---|---|---|
| **1.00× at T=1–2, 1.06× at T=3, 1.16× at T=4** in the deployable cpuset | **B** | S54. Supersedes S53's 1.52× and my earlier 1.80×, both on unreachable cores |
| Whole-SoC (unreachable): 1.17× at T=4, 1.52× at T=6 | **B** | S53 — **one run, thermal uncontrolled**, its own caveat which v1 omitted while reciting its strengths |

## DEAD — added this round

**"Verification is nearly free because reduction is deterministic" is no longer a differentiator.** Acurast made verification nearly free *without* determinism: TEE + hardware key attestation + slashing, no second run — 366+ slash references against **0** occurrences of quorum, redundancy, challenge or dispute. What survives is narrower and is now the whole pitch: **their model requires trusting Qualcomm/Google/Samsung silicon and a centrally-maintained revocation list; ours requires trusting nothing.**

**"Metering time is the wrong unit for a device you cannot audit"** (`STATE_OF_THE_UNION`) — Acurast prices per millisecond (`marketplace/src/lib.rs:877`). True only under *our* trust model; attestation makes time auditable. Conditional claim published as general.

## DEAD

The seal's 13/13 · "prefix coverage is the driver" · "bad shaping == no shaping" *as argued in S48* · the *retraction* of "≤16% left" · "the headroom is scheduling" (mechanism only) · "59% of T=8 was pthread_create" (41%; subtraction invalid) · "stage 2 is 13 ms of exec()" (5.66 ms; control timed a SIGABRT tombstone) · "the read roof declines at 8 threads" (spawn inside my own timed region) · "22.6 GB/s/core at ~2 IPC" (DVFS artefact, IPC fitted) · "residency buys nothing" (true at T≤2 only) · "no defensible multi-thread number exists".

**Removed:** "core placement 2.26×" → magnitude survives. **Half-removed:** "bundling 54×" → the clustering half is superseded; **bundling's own magnitude has never been measured on real data** and moves below.

| **Golem (clay) refuses quorum too: the REQUESTOR audits a random ~3% of the work** | **E** | `elders/yagna` @ `develop`, GPL-3.0 spec-only. `quorum`/`consensus` = **0 occurrences**; `crop_generator.py:8` `CROP_RELATIVE_SIZE=0.1` (1% area/crop) x `crops_count=3`; origins unseeded `random.uniform` so the provider cannot predict the audit; conjunctive verdict; `NOT_SURE` is a first-class state. **Second production system to refuse replication** (after Acurast). Requestor-side audit is un-Sybil-able — one requestor per job — which **deletes** the S69/S70 residency coupling rather than fixing it. We are strictly better on the oracle (byte `==` vs their trained sklearn classifier, needed because rendering is stochastic) and blocked on decomposability (reduction is sequential; a random interval needs state at step k = **S68**). **Reframes the checkpoint descope: the state commitment now has two customers, not one.** Read from source, nothing run | `reports/REPORT_Golem_clay_verification.md` |

| **Job N differs from job 1 inside one process — a fresh `metta_t` is NOT enough** | **A** | M1.1c on device, 40 reps/program, stock vs patched. `(pair $z $z)` matched by `(pair $x $y)` returns `($x $x)` or `($y $y)` **by position in the process**; positive control `!(new-space)` gives **40/40 distinct** on stock and STABLE once patch 02 lands, so the instrument is proven able to see it. **Settles PORT_PLAN M1.3's process-per-job requirement in the negative for WorkManager**: derivation (1) atomspace pollution is cured by a fresh runner, derivation (2) process-global `NEXT_VARIABLE_ID` is not. **Class characterised over 20 programs, and the proposed ban predicate is REFUTED.** 'No atom may contain the same variable more than once' fits the A/B/C/D rows but **rejects 51% of hyperon's own corpus**, with 21 of 34 rejections inside `(= ...)` bodies that measured STABLE. The E-series then found a **second mechanism no data-side rule can catch**: `VariableAtom::name()` embeds `#{id}` and `Display` calls it, so any result containing a data-origin variable prints the **process-global counter** — `(implies (Frog $x) (Green $x))` with a NON-aliasing query gives **40/40 distinct**. So there are two mechanisms: representative selection, and the counter reaching output. The second is an output-content hazard that defeats result-hash comparison directly — and it is now **FIXED at our comparison boundary** by `harness/canon.py`, which renumbers variables by first appearance: E1 goes **40 distinct -> 1**, while the heap-address control stays **40/40**, so the canonicaliser is targeted rather than signal-deleting. Renumber, never strip: `($x#1 $x#2)` and `($x#1 $x#1)` are different answers and collapsing them would let a wrong result compare equal to a right one. Wired into `q3.py`'s agreement key. **Mechanism 1 also closes, under alpha-equivalence**: `($x $x)` and `($y $y)` are the same term up to renaming, and `canon_alpha` collapses **every** mechanism-1 case to one hash (A1 2->1, A2 3->1, A3/A4/D3/E2 ->1) while the heap-address control holds at 40/40. **Opt-in (`q3.py --alpha`), not default**: `canon` removes process history, `canon_alpha` changes the notion of equality — it discards the requester's variable names and equates `($x $y)` with `($y $x)`, recorded in `canon.py` as documented loss rather than a correctness claim. Structure survives: `($x $y)` never equals `($x $x)`. The upstream per-runner id space is still the correct fix — alpha buys agreement by weakening the question. Distinct outcomes **equal** the number of aliased variables (3-way gives 3). Distinct outcomes **equal** the number of aliased variables (3-way alias gives exactly 3). Repetition in the *pattern* (B3), across *atoms* (D4), or made by a *rule* (C1/C2) is all STABLE. First device reproducer for Issue 3, which was filed with no patch | `spikes/M1_1_android/SOAK_RESULT.md` |

| **Quorum-of-3 accepts a nondeterministic job 21.5% of the time — it launders nondeterminism rather than detecting it** | **A** | M1.8b, 200 trials through the real comparison path on `test_gnd_conv.metta` (`(flip)`): UNANIMOUS 0, **MAJORITY 43 (21.5%)**, NO_QUORUM 157. Two honest devices coincide by chance and an arbitrary answer is recorded as canonical. **Makes the S59 static ban list a SAFETY control, not hygiene** — every argument treating it as tidy-up is wrong. More replicas do not fix it: a low-entropy output makes coincidence MORE likely (a single boolean would reach majority ~75% at n=3). M1.8's first run saw NO_QUORUM once and the write-up called it "known and explained" — that was the 78.5% branch observed once and treated as the behaviour | **CLOSED**: `harness/bansurface.py` gates admission, surface enumerated from `builtin_mods/random.rs:99-132`, **not blanket** — a seeded `(new-random-generator 42)` is ADMITTED because LEDGER grade A says seeded is bit-identical across ISAs. Both error rates measured: **corpus 67 -> ADMIT 66, REJECT 1**, catching exactly the laundered program and nothing else. Pipeline now **66/66 UNANIMOUS**. Syntactic only, so build-level removal stays the primary control | `spikes/M1_8_quorum3/NONDET_RESULT.md` |

| **Quorum SHRINKAGE is a craftable availability attack; the pipeline was laundering it** | **A** | M1.8c, reproduced. `hyperon` panics above **1024 results** (`TK_MAX_EXPRESSION_SIZE=1<<10`, two distinct panic sites, exit 134). Result cardinality depends on the shard and **the job author picks the program**, so a job can be authored to kill honest workers and leave the attacker holding the quorum — **cost: one crafted job, no stake, no Sybils, no collusion. Q1's 72% capture assumed quorum SIZE was fixed.** Seat-allocation defences (`DuplicateSourceInMatch`, spread, stake) bound who gets seats, none bound how many SURVIVE. Admission cannot catch it: cardinality is not knowable from source. **Fixed with one field** — `adjudicate` returns dispatched/returned and a short quorum is `REDUCED_QUORUM`, never agreement. Also found: a CRASH envelope's None `fuel_used` **crashed the coordinator**, and agreed-failure was being counted as accepted | `spikes/M1_8_quorum3/QUORUM_SHRINK.md` |

| **`collapse` aborts the process at >=1024 results — an availability defect, not a nondeterminism one** | **A** | Reproduced on `3f76dc4`, two ISAs, prebuilt and fresh builds: n=1023 OK, n=1024 `assert!(size < TK_MAX_EXPRESSION_SIZE)` at `trie.rs:539`, exit 101 / SIGABRT. `TrieKey` packs child count into 10 bits so an expression holds <=1023 children, and `collapse` builds ONE expression per result set — the bound is on **result cardinality**, chosen by the query author. Root cause of the quorum-shrinkage attack. Ask: return an error atom, as `RESULT_FUEL_EXHAUSTED` already does. **CORRECTED — the precedence bug at `trie.rs:545` IS reachable and IS a defect.** Earlier marked 'reachability unproven'; the reproducer was wrong, not the hypothesis. A **conjunction with a broad leading pattern** pushes a hashable index past 1024, the mis-decode unwraps `None` at `:179`, and the process aborts. **Symptom: reordering two patterns of the same conjunction flips a correct answer into a dead process** — n=3000, 50 results either way, selective-first OK / broad-first PANIC. One-character fix (parentheses); 298+29+6 tests pass. This is a SECOND, independent defect from the `:539` size assert, which survives the fix | `proposed/hyperon-result-ceiling/` |

| **Quorum agreement must be counted in failure DOMAINS, not seats — and this project has never run a 3-domain quorum** | **A** | Reported by review, implemented, and it **refuses our own headline run**. `REDUCED_QUORUM` catches workers that die; it cannot catch workers that were never independent — dispatched 3, returned 3, domains 2. Two workers on one binary and one host share libm, clock, page tables and the 1024-result panic, so their agreement is nearly free. Host-only: `INSUFFICIENT_DOMAINS 3/3 1dom, accepted 0/4`. Real M1 setup is `3/3 2dom`. **Q1's 72% capture runs on domains, so this compounds with it.** Domains counted among AGREEING workers only. Domain key `host|bin:sha256`, after k8s `podtopologyspread` topologyKey/maxSkew. Reclassifies 'we have one phone' from a hardware gap to a **validity** gap. **Extended to a per-fault-class vector** (binary/host/os/isa/operator), binding on the WEAKEST axis: `binary 2, host 2, os 2, isa 1, operator 1`. Two disclosures invisible in the scalar — **operator=1 is the axis Q1's 72% is actually about**, and **isa=1 means this quorum re-tests nothing of S57's cross-ISA result**. The isa axis first read 2 because macOS says `arm64` and Android `arm64-v8a`: the same ISA, two strings — the 'key that flatters itself' defect committed inside the fix for it. **Domain key was WORKER-DECLARED — third instance of self-reported identity as a defect** (after S62 `backend_class` INVALID and M1.5's binary hash). A dishonest worker emits distinct host/operator strings and inflates the count, capturing the quorum *through the field built to prevent it*. Fixed as a category change: domain is now **observed coordinator-side or pinned**, never declared — `binary` hashed from the dispatched file, `host` from our own adb connection, `operator` pinned to `UNATTESTED` because no attestation root exists. Declarations are still collected and diffed, so a worker buying a domain is visible (fired on first run, benign). **Today's refusal does not depend on this**: operator=1 and isa=1 hold regardless because every worker is ours — self-reporting becomes load-bearing on day one of a real fleet. Also records fault classes NO key separates (shared implementation bug; the G18 1022 panic, identical on both machines), where quorum is structurally incapable at any size | `spikes/M1_8_quorum3/M1_CHAIN.md` |

| **COI audit: residency CONVERTED to observed; timing RESISTS conversion, and fuel is the right axis instead** | **B** | A22 applied field-by-field to `hyperjob_v0` and the live envelope. Cross-checking is what makes a self-reported field safe: `status`/`fuel_used`/`result_hash` sit in the quorum key so a lone liar diverges, but **anything outside the key is unchecked** — `Timings` (`:140-143`) will gate scheduling and a device profits by understating cost. **The hole not yet built:** `prefer_cached_cids` (`:114`) needs a device's cache contents and the schema has no field for it, so the default implementation is a worker telling the matcher which jobs it should win — a direct route to S69/S70 residency capture. **Observed alternative already exists**: **Now implemented**: the coordinator reads the device shard dir directly — live run `66 of 66 held, worker claimed 0 fetches`. **Timing resists conversion**: coordinator-observed is 18,123 ms at 16 jobs/session (our own batching) and still 193.8 ms vs 68.2 ms declared when serialised — ~3x coarser than the quantity it would check, so it cannot detect the lie it exists to catch. **A23 sets a floor on A22's remedy**: 'observe it instead' fails where observing adds more noise than the misreport. Right answer is to police **fuel**, which is already in the agreement key and cross-checked by construction | `spikes/M1_8_quorum3/COI_AUDIT.md` |

| **Every check in the M1 chain now states its detection floor** | **B** | A check whose floor is unstated reads as verification when it is a bound. Exact (coordinator holds the reference): REDUCED_QUORUM, domain mismatch, canon. Floored: timing **2.8x** — cannot detect the lie it exists to catch. Blind: quorum agreement to **shared bugs** and to nondeterminism at **21.5%**; admission to `py-atom` resolving a runtime string (undecidable); residency to a **compromised device shell** (exact against an honest one — it moves trust from our worker to the device OS, which is an improvement, not verification); provenance proves 'could not have come from here', never 'did'. Two failure kinds: **A21-family** (instrument coarser than the deviation) and **A22-residual** (check exact but rests on a party that may be the adversary). Neither is fixed by running the check harder | `spikes/M1_8_quorum3/DETECTION_FLOORS.md` |

| **Device-side shard integrity closed, and ISA monoculture closed** | **A** | Worker now `sha256sum`s the cached blob **on device** before running — the CID is the hash, so no extra metadata. **Control run, not argued**: corrupted an on-device shard (sha `6e99de46` -> `3398b5c2`), worker rejected it, refetched 2002 B, result correct at 3/3 fuel 58. Previously a corrupt shard was caught only by three devices disagreeing — and not at all if corruption were identical across them. Separately, added an x86-64 Rosetta worker: **isa 1 -> 2, binary 2 -> 3**, restoring the cross-ISA property S57 had and the quorum had lost; aarch64 and x86_64 agree byte-identically **inside the live pipeline**. The mismatch detector caught a REAL error here — the worker declared `isa=arm64` (its own process) against an observed `x86_64` binary, which would have merged two ISAs into one domain. `operator` still binding at 1 | `spikes/M1_8_quorum3/DETECTION_FLOORS.md` |

| **Locality routing measured on a real fleet: S61's tension is real under SKEWED demand and a `maxSkew` cap buys out of it** | **A** | M2.1. N real worker processes, own caches, real bytes, 198/198 unanimous every arm. **Uniform demand shows NO tension** (locality_pure 2.00x vs random 3.17x) — and that was nearly published; it is an artifact of dispatching each program once so no shard is hot. Under Zipf: **1.4 -> locality_pure 19.33x imbalance vs random 1.79x**, transfer saving flat at 89-96%. Least-loaded tiebreak halves it (8.38x), not enough. **k8s `maxSkew` ported to load fixes it: knee at maxSkew=1 -> imbalance 1.06x (BETTER than random) at 78.7% transfer saved.** 0->1 buys 40 points of transfer for 0.03x imbalance. Cap refills from the ineligible set so it can never shrink the quorum (M1.8c). One failure domain — routing only, says nothing about trust | `spikes/M2_1_fleet/RESULT.md` |

| **Locality routing MANUFACTURES the low-coverage regime Q1 is weakest against** | **B / sim** | M2.1 §5, residency accumulating from execution only, no prefill, 12 rounds. Pure locality reaches **zero transfer by round 7** and its **19x imbalance never decays** — a ratchet, since nothing makes a non-holder resident. **FALSIFIER RUN AND IT FIRED**: coverage stalls at **exactly k** (k=3->3.00, k=5->5.00, k=7->7.00 hot-shard), so the '~3 = Q1's worst case' link was a coincidence of both being 3 and is **refuted as stated**. The corrected law is worse: **pure locality pins holders-per-shard to exactly the quorum size**, so there is no redundancy beyond the quorum — an operator holding those k devices captures **100%** of that shard's jobs, not 72%, and fleet growth cannot help because a non-holder never becomes resident. Q1's arithmetic assumes pool > quorum; under pure locality pool == quorum. Cap restores it where it matters: hot-shard coverage **7.38 vs 3.00 at k=3**. **Both further falsifiers now run and both strengthen it.** (A) The ratchet does **not need skew** — coverage pins to exactly k at every zipf including **0.0/uniform**; only imbalance is skew-dependent. **This corrects §1**, which concluded 'no tension under uniform demand' from an imbalance-only measurement and never looked at coverage. `pool == quorum` is inherent to pure locality, not a skewed-demand pathology. (B) It does **not need dense load** — coverage pins at k across a 25x density range, and imbalance **explodes to 89x at fleet 64 / 79x at fleet 198**, so fleet 16 was the flattering case and a real fleet is the bad end. Cap degrades too: at fleet 198 it holds hot coverage at 23.88 but imbalance rises to 6.0x, so `maxSkew` needs per-fleet tuning. **Unfalsified**: 12-round horizon only, and device churn is unmodelled — churn is the obvious escape from a ratchet. `maxSkew=1` fixes the dynamic not just the snapshot: 1.06-1.15x across all rounds, coverage still climbing, 3 fetches by round 11. **A spread floor is a safety property, not a tuning knob.** SIMULATED (sections 1-4 executed; this one models routing only). **Falsifier stated and not yet run**: coverage stalls at ~3 = quorum size k=3, so re-run at k=5,7 — if it stalls at k each time the Q1 link is a coincidence of both being 3 | `spikes/M2_1_fleet/RESULT.md` |

| **M1.7 phone-initiated transport works: 66/66 over HTTP, byte-identical to host** | **A** | Device dials out only (`GET /job` long-poll, `GET /shard/<cid>`, `POST /result`); coordinator binds **127.0.0.1 only** and the phone reaches it via `adb reverse`, so there is **no LAN exposure and nothing leaves the machine** — S8's 'the phone cannot accept inbound' satisfied by construction. 66 jobs cold cache, 20.8 s, 177 KB, 65 OK + 1 FUEL_EXHAUSTED, **66/66 identical on fuel_used AND sorted_hash**. **The miss control FAILED first and caught a real bug**: `curl -s -o F` writes the empty 404 body and exits 0, so the agent ran fuelrun on an empty file and posted a result for a shard it never received — the `da39a3ee` empty-capture family through a new door. Fixed with `curl -fsS` + empty check; re-run gives misses 1 / results 0. **M1.8's device_fetch already had this check and I did not carry it into the new transport — a check that exists is not a check that applies** | `spikes/M1_7_transport/RESULT.md` |

| **Process reuse is SAFE for the measured job class — M1's largest open issue closes** | **A** | M1.3b. `soakrun.rs` runs many DIFFERENT jobs in one process with a fresh `Metta` each (so derivation (1) is handled and only the process-global `NEXT_VARIABLE_ID` is under test). 31 probe runs interleaved with 30 other jobs: **31 distinct RAW hashes, 1 distinct CANON**. Position changes the bytes every time and `canon` erases it. Aliasing probe needs `canon_alpha` (2->1), **but alpha is lossless only on ground results and an aliasing result is never ground** — so that class stays unsafe and its admission gate was already refuted at 51%. **The entire 67-program corpus returns ground results**, so WorkManager reuse is usable for every job executed here. Caveat: `fuelrun`'s own `raw_hash`/`sorted_hash` are computed pre-canon and remain position-dependent | `spikes/M1_3b_process_reuse/RESULT.md` |

| **`maxSkew` is the transfer/redundancy dial, not a perf knob — and §3's 'knee at 1' is withdrawn** | **B / sim** | M2.1 §6, steady state 12 rounds. Imbalance and hot-shard coverage are **one axis** (both are spread), traded against transfer. Fleet 198: `maxSkew=0` -> pool **59.1**, imbalance **1.00x**, still **−61% transfer** vs random; `maxSkew=1` -> pool 23.9, −83%; pure locality -> pool **3.0** (= quorum), −93%, capture free. **Hot-shard coverage IS Q1's honest pool**, so the setting is a security choice, not a performance one — pick the pool your threat model needs and pay the transfer. §3's 'knee at maxSkew=1' was **single-round with prefill** and ignored redundancy, which only accumulates; 0->1 also costs **60% of the honest pool**. Relative maxSkew (fraction of mean) is worse than absolute 0 everywhere | `spikes/M2_1_fleet/RESULT.md` §6 |

| **The charging gate accepted an UNPLUGGED phone all session — MISSION_LOOP §10 was not honoured** | **A** | `BATTERY_STATUS_FULL` means the battery is full, not that the device is plugged in: an unplugged phone at 100% reports `status=5`, which both `quiet.sh --device` and M1.3 preflight accepted. Surfaced only because **WorkManager refused to run** — `Unsatisfied constraints: CHARGING` while our gate reported success in the same minute. Ground truth: all four powered flags false, `Max charging current: 0`, `deviceidle get charging: false`. The old rule had an explicit rationale (chargers disengage at 100%) that is **real but does not imply the conclusion** — status cannot separate plugged-and-full from unplugged-and-full, so it was *incapable* of being right. **Determinism results unaffected** (byte-identity does not depend on power). **All on-device TIMING is now unknown-condition** — battery DVFS differs — including M1.5b's transfer curve and M1.1's 98.5 µs preflight. Fixed by asking `deviceidle get charging`, the same signal JobScheduler uses; quiet.sh now exits 1. **A22 applied to ourselves: the gate was written by the party that benefits from it passing** | `spikes/M1_3_worker/CHARGING_DEFECT.md` |

## NEVER MEASURED

| gap | note |
|---|---|
| **`fuelrun` runs unpinned** | `Metta::new(None)` executes the host config dir's `init.metta`/`environment.metta`. Inert on this Mac so it did not confound S57/S58, but it is a user-writable silently-executed input. Fix: `EnvBuilder::test_env()` |
| **Bundling on real data: B=16 fits an 800k shard in VTCM at p90 0.2% store checked** | **B** | **B1.** The requirement is **32×**, not the 54× assumed nor the 16× B1 first reported — and **B1b revises the operating point to B=32**: at B=16, 7.66% of dimensions are exact ties all broken to 0, giving a 4.6-point sign imbalance — the worst of the VTCM-viable options. B=32 costs 0.3pp of p90 shortlist and buys 2.4× fewer ties, 3× less imbalance and 2× VTCM headroom (3.20 MB). Bias is benign for **ranking** (measured) but adds to the fixed-cutoff problem S52 already records. **B1c:** 1 bit/dim is verified against `realkg.c:152-162` and is therefore a correct model of the *implemented* encoding — but `sign(Σ)` is ternary and the implementation discards `sign(0)`. Under a sparse zero-mask (11.30 MB) or a full second plane (12.80 MB) **B=16 does not fit VTCM. B=32 fits under all three encodings.** Two independent routes — sign-bias and encoding-robustness — reach B=32 | Curve nearly flat to B=32. Provenance: B=1 reproduces S11 exactly (34.83 MB = 102.4 × 272115/800000) and B=64's 0.17% median matches **S52's measured 0.2%** on a different instrument. **The unmet link in the NPU chain now holds** — the descope rests on the ladder alone, with no unanswered technical objection |
| **`verifier2.py` untested by anyone but its author** | 17 self-authored cases — *exactly v1's evidentiary profile*, and v1's 13/13 contained a test that never called the verifier. Grade **E**, highest-risk artefact here |
| **`hyperjob` carries no RNG seed and no `rand` crate version** | S58: seed pins the run, but `StdRng`'s algorithm is not guaranteed stable across `rand` versions, so both must be declared. Also untested whether *all* seeds are ISA-stable — one was |
| **`hyperjob_v1.proto` still declares `quant_scale` as `double`** | Now unambiguous: production practice at 260k devices uses an exact rational. Defect, not a gap |
| **No stake funding model for a phone** | Devices have no capital. Acurast solves it with third-party delegation (`offer_backing`/`delegate`); `PORT_PLAN` and `PROPOSAL_DRAFT` never pose the question |
| **No acknowledgement step in `hyperjob`** | Acurast is two-phase: `propose_matching` → `acknowledge_match`. A phone can vanish mid-negotiation |
| **No cleanup/GC budget in M3** | Four `cleanup_*` extrinsics are ~⅓ of Acurast's marketplace surface. On a phone fleet abandonment is the common case |
| **Commit registry has `close()` but no clock** | No real deadline |
| **Worst-case recall** | S17: 0.97 mean with a **0/100 minimum** — at least one query loses everything. Nobody has tuned for worst case |
| **Co-tenancy does not perturb results — SEPARATE PROCESSES only** | **B** | 8 concurrent **processes**, identical digest at every N. **This does not cover in-process concurrency**, and there is now a concrete reason to expect that to fail: `NEXT_VARIABLE_ID.fetch_add(…, Relaxed)` keeps ids unique but makes *which* thread gets which id scheduling-dependent. Each process starts its counter at 1, which is exactly why this passed. Untested | S32a table, recovered from `archive/chat.log:1560-1568`. Evidence a phone can serve multiple requestors without cross-contamination; nothing else here touches it. **Not** the same property as S51's thread-decomposition invariance — `fuelrun` is single-threaded, so S32a decomposes nothing. Survives S54: a hash does not care which core produced it |
| **S32 is the worked example for per-claim INVALID** | — | Split verdict inside one spike: **timing columns INVALID** (fleet projection, plus 8-way cores S54 proved unreachable), **digest column B and promotable**. Grading the spike rather than the claim would have lost the good half |
| **S32's 28,700 jobs/s** | **INVALID** | **RESULT.md now written (2026-08-16), verdict RED as a citation source.** 28,726 = 10,000 devices × 2-of-2 quorum, i.e. a projection; per device ~2.9 jobs/s. `tps.py` claims 5.87× scaling but `tps.json` gives 7.71× burst / 6.01× sustained — **not derivable from its own data**. All `eight_way` rows are on cores S54 proved unreachable. Previously: `tps.py` still prints it under "MEASURED"; S33/S34 consume it. S34's "1.2× short" inherits it, so its NPU-necessity conclusion is unsupported |
| **Step-level bisection IS reachable on hyperon's public API** | **B** | S60 v2. `impl Debug for RunnerState` (`mod.rs:527-534`) exposes `interpreter_state` incl. the plan stack; it changes on **50,786 of 50,794 steps** (corpus median 1.0000). Also `interpret_init`/`interpret_step` and `Metta::space()`. v1 claimed the opposite |
| ~~Checkpoint commitments reproduce *because of* the patches~~ | **INVALID** | **Falsified by revert test.** Stock `3f76dc4` with zero patches gives **byte-identical roots** on all six programs. The patches were not load-bearing for S65's result; S65's "two different determinism regimes" framing is unsupported |
| **S65's commitment is FORGEABLE — it binds no computation** | **A** | **100% of leaf content is `"()\n"` repeated** across all six programs; content bytes are exactly `3·k(k−1)/2` in every case. The root is a function of ~22 integers, and was reproduced in 6 lines of Python without running hyperon. `current_results()` is emitted output, not interpreter state — two executions with divergent stacks and the same output prefix commit identically. **A bisection game over this has nothing to bisect** |
| ~~Checkpoint commitments now REPRODUCE~~ | superseded | **S65**, and this is what the hyperon patches bought: 6 corpus programs, every run one Merkle root. S60 could not achieve this — its digests varied because `Display` leaked heap addresses. Checkpoint hashing, bisection and dispute proofs were all genuinely blocked behind that fix |
| **Checkpoint retention measured the wrong thing** | **D** | 192–1,088 B is **leaf digests only**. An opening at position *k* is `(preimage_k, path_k)`; `ckpt.rs` drops every preimage, so a verifier can be shown only that an opaque hash is a tree member. Real openable retention for `c1` ≈ **1,573 B** and scales with output volume, not checkpoint count. Also, leaf[0] is a bootstrap artefact (`prev_len` starts at `usize::MAX`) |
| ~~Checkpoint retention is hundreds of bytes~~ | superseded | S65: 22 checkpoints in 50,794 steps → **704 B retained, 21 inner hashes, 5 bisection probes**. Corpus range 192 B–1,088 B, 3–6 probes. S60's "32N bytes = 1.6 MB" assumed a checkpoint **per step**; they fire on observable state change only |
| **A dispute resolves to an interval of up to 3,309,427 steps** | **B** | S65 reported the *mean* gap over 6 hand-picked programs. A dispute lands in the interval containing the disagreement, so the quantity is the **max**. Full 24-program sweep: mean 168–1,103,188, **max 256–3,309,427**. Even within S65's own six the max is 29,742 — 4.7× its stated ceiling; corpus-wide it is **525×** off. The "one-interval proving target is 1k–6k steps" input to S66 is wrong |
| ~~A dispute resolves to 1,025–6,298 steps~~ | superseded | S65. So the risc0 figure to measure is **one-interval proving**, not one-step. Interval is tunable downward at ~32 B and one hash per extra checkpoint — commitment is nearly free, so the trade favours more checkpoints |
| **Bisection commitment THROUGHPUT cost is UNPRICED** | **D** | S60 v1's 11.6% timed a **5,709-step aborted re-run** — the harness reused one `Metta`, so iterations 2+ ran against a polluted space and terminated on an assertion error that `run_step` returns `Ok(())` for. Honest run: `lazy` 37.6%, and ~94% of that is `to_string()` not hashing; an O(1) probe gives 4.1%; a true per-step plan commitment costs **200×**. Cost is also **O(n²)** in accumulated results — 24.8× on a real payload |
| `Variables({…})` hash-set iteration order is a **second** nondeterminism source | **B** | S60. After masking every `0x…` address, 386 diff hunks remain between runs of the same program. `proposed/hyperon-nondeterminism/` covers addresses and `intersection-atom`, not this |
| A hash chain cannot support post-a-root-then-open-at-k | **B** | S60. Fine for two-party interactive bisection (Arbitrum-style); fatal for R-NEW's shape. Needs a Merkle/vector commitment: ~2N hashes plus **32N bytes retained**, 1.6 MB per 50k-step run. Unmeasured |
| **MeTTa's reduction path is fs-clean, but "bounded to the runner boundary" is wrong** | **D** | The fs-location claim holds — all fs sites are in the runner across 8 files, `interpreter.rs` and `stdlib/` have zero. But the port is **not** a compilation unit: `GroundingSpace` lives in `lib`, not `hyperon-space`; `interpreter.rs:9` imports `crate::metta::runner::stdlib::core::IfEqualOp`; and there is no feature gate dropping `runner`, so building the interpreter builds the whole crate. Carving it requires a fork. Mandatory deps also bring `thread_local!` via `rand` and `env_logger` |
| ~~MeTTa's reduction path is already zkVM-clean~~ | superseded | **S66.** `hyperon-atom`/`-common`/`-space` and `interpreter.rs` use **zero** filesystem, threads, clock or network. All 75 fs sites are in the runner (module loading, catalog, `fileio`), which a dispute proof does not need — bisection starts from a committed state, so the guest never loads a module. The port is bounded to the runner boundary |
| hyperon **spawns** no threads, but is not free of threading primitives | **B** | S66's grep searched `std::thread`, the one primitive genuinely absent. Present: `hyperon-common/src/unique_string.rs:8` — a `LazyLock<Mutex<UniqueStringStorage>>` taken on **every** symbol/variable name construction; and `hyperon-atom/src/lib.rs:222-226` — `static NEXT_VARIABLE_ID: AtomicUsize`. **Corrected scope:** the id never reaches printed output (`Display` at `:335` prints `${name}` only), so it does not break output hashing — the 67-program corpus is consistent with that. It *does* participate in `VariableAtom`'s derived `Hash`/`Eq`, and `VariableAtom` keys `matcher::Bindings`, so **binding-map iteration order depends on process history**. Confined to `make_unique()` (`:330`); `new()` leaves `id: 0`. Only `hyperon-space` is clean |
| **My own patch 02 briefly falsified this** | **fixed** | The `stable_id()` version added `Mutex` + `OnceLock` to `space/grounding/mod.rs` — the space every reduction runs on. Replaced with identity-free `Display`; no global state added |
| **zkVM prover cost for a MeTTa trace** | The gating unknown for the succinct-settlement route. Proving runs ~10⁵–10⁶× native; **a phone cannot prove**, so this relocates cost rather than removing it. Unmeasured |
| **`risc0/groth16` `no_std` / Substrate pallet viability** | Our chain is not EVM. Rust verifier exists; nobody has checked pallet compatibility or its PoV |
| **Whether a results-only commitment is sound at all** | S60 commits over `atom.to_string()`, which S58 showed embeds heap addresses and is subject to `HashMap`-ordered variable naming. **Unsound today** — blocked on `proposed/hyperon-nondeterminism/` |
| **Byte-compare Q4_0 logits from oflineAI's own NPU path** | **Now the cheapest decisive experiment in either repo.** The engine, the device and the models already exist; it needs a logits dump and a diff. Q4_0 carries **per-block float scales**, so the compute is not pure integer and is exposed to exactly the divergence S59 measured. Two runs, one device first; then two devices |
| **Bit-exactness of quantized inference ACROSS NPU vendors** | **The vendor-level version of the above, and it supersedes the TOPLOC one.** Integer matmul is associative, but **requantization** (int32→int8) is a multiply-shift whose rounding a vendor may implement differently, and saturation order can differ. Qualcomm HTP vs Samsung vs MediaTek is unmeasured. If it holds, byte-comparison verification covers neural work with **no TEE and no ZK** |
| **Whether TOPLOC tolerance absorbs the S59 divergence** | The decisive experiment for the modular architecture. S59 measured max **2 ULP** across ISAs; TOPLOC's exponent-exact/mantissa-tolerant comparison should absorb that trivially — mantissa drift with an identical exponent is precisely its design case. **Nobody has checked.** One spike |
| **No GNN in any elder** | Message-passing graph networks are absent from all 32 repos; torchhd is VSA, not graph convolution. If message passing is wanted there is nothing to port |
| **Fixed-point attention-broker conversion** | Proposed in `analysis/GRAPH_AI.md`, not measured |
| **Upstream ask: `cfg`-gate the math-op registrations** | Second instance of the M0.2 minimal-build request (already filed in the backlog for `builtin_mods/json.rs`). Not written |
| **Any NPU code** | And **VTCM is 8 MB vs a 12.8 MB packed store — it does not fit**, so bundling is a *prerequisite* for residency |
| **Energy per job** | *diagnosis was wrong*: not root. Battery sat at 100% and plugged, so the charge counter is static. Needs wireless adb + physical unplug, or a USB power meter |
| **Android vendor-libm variation across devices** | S59 shows every libm build is its own equivalence class. A fleet of Androids with different vendor libms could diverge with no ISA change. More product-relevant than the ISA question and untested |
| **The settlement layer has never been costed** | **Gating, and possibly the biggest hole in the plan.** Report-shaped per-job settlement caps at **17.1 jobs/s** (PoV-bound, re-derived from Acurast's runtime), **8.6/s** with the replication we require, against S32's 28,700 device-side. **3,353× shortfall.** PoV binds by 2–3 orders over ref_time, so a faster chain buys nothing — only smaller or fewer proofs. See `RISKS.md` R-NEW. **Answer identified**: Groth16 proof is **256 bytes, constant in the size of the computation** (measured from `risc0` `verifier.sol`, 4 pairings via precompile 8). Batching collapses per-result cost to `PoV_batch/N`. Combined with proof-on-challenge, the happy path posts **zero** proofs. Gating unknown is now **prover cost**, not chain cost |
| **Shard-parse vs query-parse** | S56 put parse at 31% of stage 2, the only amortisable part, but its 13-expression program cannot separate the two |
| **WorkManager limits** | ~10 min/worker, 6 h per 24 h dataSync |
| **Play Integrity 10k/day** | fleet attestation ceiling |
| **Sustained vs burst** | S30: 3.7× host:phone sustained (not 2.7×), ~60% of burst single-thread; multi-core never measured, S51 ended throttled to 1,996 MHz |
| **A fixed, non-oracle cutoff** | every bundling result fits the cutoff to the answer |
| **3 workers + 1 coordinator** | the deployable shape; S54 put the coordinator outside the background set |
| M1.1/1.3/1.5/1.7 | app, worker, shard store, transport |

## Standing rules

1. **Report cycles/row, not GB/s** — GB/s is a function of the governor. *Rows above still violating this are marked.*
2. **Read the compiler warnings.**
3. **Never subtract a separately-measured overhead** — measure the controlled pair.
4. **Measure the null and prove it can fire.** Mine was eliminated dead code.
5. **Label oracle-fitted parameters on the row, with their cost.**
6. **One draw is not a measurement, and one configuration is not either.**
7. **Never let a retracted instrument supply a comparand.** The 5.66 ms denominator came from the attack that retracted S45's 13 ms — and that attack disqualified its own harness two lines later (*"the harness doing nothing costs 6.0–6.9 ms"*). **5.66 is below the stated noise floor of the instrument that produced it**, it is listed in this ledger's own broken-controls line, and I hardcoded it as ground truth anyway.
8. **Variance can be process-scoped, and repetition cannot see it.** I diagnosed a 1.7× spread as DVFS; it survives a pinned, settled clock (0.302/0.275/0.170 at an identical 2,918,400 kHz). Within a process, CV ~3%; across processes, CV 23%. Rule 1's "report cycles" does **not** fix this — cycles inherit the process's placement. Sample **N processes**, not N iterations. A tight within-run MAD is evidence of nothing.
9. **Verify the control, gate it on plausibility.** **Five** were silently broken: an eliminated null loop; a clock calibration folded to a closed form (769,190,472 MHz); a `date`-bracketed timer costing two process spawns; a coordinator sharing a core; a null bracket understating by 1.3×.
10. **Finding a failure mode does not inoculate you against it.**
11. **Measure the configuration the product can reach, before optimising one it cannot.**
12. **A retraction must be applied to every file that carries the claim.** S55's correction landed in `LEDGER.md` and nowhere else; `LEDGER.md:63`, `GAP_MATRIX.md` row 7 and `proposed/mork-license/README.md` all still asserted "uncallable" hours later, the last of them being the artefact intended to go upstream. One-file retractions are how a dead claim keeps voting.
13. **An E-grade claim must never be load-bearing.** "MORK has no library surface" was flagged as unverified and still gated the largest cost in the query path for a week. If an unmeasured claim gates a decision, promote it to a measurement before deciding.
